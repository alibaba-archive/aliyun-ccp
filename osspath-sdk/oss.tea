/**
 *
 */
module Client {

  type @getHost = (string, string): string
  type @getProtocol = (string, string): string
  type @isStatusCode = ($Response, number): boolean
  type @readAsJSON = async ($Response): object
  type @toJSONString = (any): string
  type @toQuery = (any): map[string]string
  type @notEmpty = (string): boolean
  type @getRFC2616Date = (): string
  type @getSignature = ($Request): string
  type @default = (string, string): string
  type @defaultNumber = (number, number): number
  type @getSysAccessToken = async (): string
  type @getAccessKeyId = async (): string
  type @getAccessKeySecret = async (): string

  type @domainId = string
  type @endpoint = string
  type @protocol = string

  model RuntimeOptions {
    autoretry: boolean(description='whether to try again', name='autoretry'),
    ignoreSSL: boolean(description='ignore SSL validation', name='ignoreSSL'),
    maxAttempts: number(description='maximum number of retries', name='maxAttempts'),
    backoffPolicy: string(description='backoff policy', name='backoffPolicy'),
    backoffPeriod: number(description='backoff period', name='backoffPeriod'),
    readTimeout: number(description='read timeout', name='readTimeout'),
    connectTimeout: number(description='connect timeout', name='connectTimeout'),
    httpProxy: string(description='http proxy url', name='httpProxy'),
    httpsProxy: string(description='https Proxy url', name='httpsProxy'),
    noProxy: string(description='agent blacklist', name='noProxy'),
    maxIdleConns: number(description='maximum number of connections', name='maxIdleConns'),
    localAddr: string(description='local addr', name='localAddr'),
    socks5Proxy: string(description='SOCKS5 proxy', name='socks5Proxy'),
    socks5NetWork: string(description='SOCKS5 netWork', name='socks5NetWork'),
  }

  model Config {
    endpoint?: string,
    domainId?: string,
    protocol?: string,
    credentialType?: string,
    securityToken?: string,
    accessKeyId?: string,
    accessKeySecret?: string,
    clientId?: string,
    refreshToken?: string,
    clientSecret?: string,
    accessToken?: string,
    expireTime?: string,
  }

  init(config: Config);

  /**
   * 
   */
  model AccountAccessTokenResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    accessToken: string(name='access_token', description='用于调用业务接口的accessToken', example='cnskjajkahwdhwialsnd', x-go-name='AccessToken'),
    avatar?: string(name='avatar', description='当前用户头像', example='cherry', x-go-name='Avatar'),
    defaultDriveId?: string(name='default_drive_id', description='Default Drive ID', example='100', x-go-name='DefaultDriveID'),
    existLink?: [ LinkInfo ](name='exist_link', description='当前用户已存在的登录方式', example='', items='{"$ref":"#/definitions/LinkInfo"}', x-go-name='ExistLink'),
    expireTime?: string(name='expire_time', description='accessToken过期时间，ISO时间', example='2019-09-01T06:57:48.813Z', x-go-name='ExpireTime'),
    expiresIn?: long(name='expires_in', description='accessToken过期时间，单位秒', example=3600, format='int64', x-go-name='ExpiresIn'),
    needLink: boolean(name='need_link', description='是否需要绑定', example=false, x-go-name='NeedLink'),
    nickName?: string(name='nick_name', description='当前用户昵称', example='cherry', x-go-name='NickName'),
    refreshToken?: string(name='refresh_token', description='用于刷新accessToken', example='cnskjajkahwdhwialsnd', x-go-name='RefreshToken'),
    role?: string(name='role', description='当前用户角色', example='user', x-go-name='Role'),
    state?: string(name='state', description='临时权限，用于登录成功后设置密码', example='klafhjas-asdasd-asd-as-d-asd-asd-asdasd', x-go-name='State'),
    tokenType?: string(name='token_type', description='accessToken类型，Bearer', example='Bearer', x-go-name='TokenType'),
    userId?: string(name='user_id', description='当前用户ID', example='DING-xxxxx', x-go-name='UserID'),
    userName?: string(name='user_name', description='当前用户名', example='cherry', x-go-name='UserName'),
  }

  /**
   * 
   */
  model AccountLinkRequest {
    detail?: string(name='detail', description='账号信息', example='{name:jack,age:20}', x-go-name='Detail'),
    identity: string(name='identity', description='唯一身份标识', example='15208333333', x-go-name='Identity'),
    status?: string(name='status', description='状态', example='normal', x-go-name='Status'),
    type: string(name='type', description='认证类型', example='mobile', x-go-name='Type'),
    userId: string(name='user_id', description='绑定的user_id', example='123987123617823', x-go-name='UserID'),
  }

  /**
   * 
   */
  model AddStoreRequest {
    basePath?: string(name='base_path', description='存储公共前缀', example='ccp-data', x-go-name='BasePath'),
    bucket?: string(name='bucket', description='bucket名称', example='data-sz-bucket', x-go-name='Bucket'),
    domainId: string(name='domain_id', description='domain ID', example='sz100', x-go-name='DomainID'),
    endpoint: string(name='endpoint', description='存储访问地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='Endpoint'),
    ownership?: string(name='ownership', default='system', description='存储归属，system表示系统提供，custom表示使用自己的存储', enum='system, custom', example='system', x-go-name='Ownership'),
    roleArn?: string(name='role_arn', description='访问Bucket的角色ARN', example='acs:oss:*:*:*', x-go-name='RoleArn'),
    type: string(name='type', description='存储类型，当前只支持oss', enum='oss', example='oss', x-go-name='Type'),
  }

  /**
   * 
   */
  model AppAccessStrategy {
    effect?: string(name='effect', x-go-name='Effect'),
    exceptAppIdList?: [ string ](name='except_app_id_list', items='{"type":"string"}', x-go-name='ExceptAppIDList'),
  }

  /**
   * 
   */
  model AuthConfig {
    appId?: string(name='app_id', x-go-name='AppID'),
    appSecret?: string(name='app_secret', x-go-name='AppSecret'),
    enable?: boolean(name='enable', x-go-name='Enable'),
    endpoint?: string(name='endpoint', x-go-name='Endpoint'),
    enterpriseId?: string(name='enterprise_id', x-go-name='EnterpriseID'),
  }

  /**
   * Base file response
   */
  model BaseCCPFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    category?: string(name='category', description='category', enum='doc, image, audio, video', example='doc', x-go-name='Category'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='DomainID', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    hidden?: boolean(name='hidden', description='Hidden
type: boolean', enum='false, true', example=false, x-go-name='Hidden'),
    imageMediaMetadata?: [ ImageMediaResponse ](name='image_media_metadata', $ref='#/definitions/ImageMediaResponse'),
    labels?: [ string ](name='labels', description='labels', example='label1:1, label2:2', items='{"type":"string"}', x-go-name='Labels'),
    meta?: string(name='meta', description='meta', example='file meta', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFileId?: string(name='parent_file_id', description='parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFileID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    starred?: boolean(name='starred', description='starred
type: boolean', example=false, x-go-name='Starred'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
  }

  /**
   * Base drive response
   */
  model BaseDriveResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    creator?: string(name='creator', description='Drive 创建者', example='ccp', x-go-name='Creator'),
    description?: string(name='description', description='Drive 备注信息', example='ccp team drive', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
    driveName?: string(name='drive_name', description='Drive 名称', example='ccpdrive', x-go-name='DriveName'),
    driveType?: string(name='drive_type', description='Drive 类型', example='normal', x-go-name='DriveType'),
    owner?: string(name='owner', description='Drive 所有者', example='ccp', x-go-name='Owner'),
    relativePath?: string(name='relative_path', description='Drive存储基于store的相对路径，domain的PathType为OSSPath时返回', example='/a/b/e/', x-go-name='RelativePath'),
    status?: string(name='status', description='Drive 状态', example='enabled', x-go-name='Status'),
    storeId?: string(name='store_id', description='存储 ID, domain的PathType为OSSPath时返回', example='123', x-go-name='StoreID'),
    totalSize?: long(name='total_size', description='Drive 空间总量', example=102400, format='int64', x-go-name='TotalSize'),
    usedSize?: long(name='used_size', description='Drive 空间已使用量', example=1024, format='int64', x-go-name='UsedSize'),
  }

  /**
   * Base file response
   */
  model BaseOSSFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    filePath?: string(name='file_path', description='file_path', example='/a.jpg', x-go-name='FilePath'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFilePath?: string(name='parent_file_path', description='parent_file_id', example='/', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFilePath'),
    shareId?: string(name='share_id', description='share_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', pattern='[0-9]+', x-go-name='ShareID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
  }

  /**
   * List share response
   */
  model BaseShareResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    createdAt?: string(name='created_at', description='created_at', example='2006-01-02T15:04:05.999Z', x-go-name='CreatedAt'),
    creator?: string(name='creator', description='creator', example='xxx', x-go-name='Creator'),
    description?: string(name='description', description='description', example='share description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='domain-1', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', x-go-name='DriveID'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z', x-go-name='ExpiredTime'),
    expired?: boolean(name='expired', description='expired', example=false, x-go-name='Expired'),
    owner?: string(name='owner', description='owner', example='xxx', x-go-name='Owner'),
    permissions?: [ string ](name='permissions', description='permissions', example='', items='{"type":"string"}', x-go-name='Permissions'),
    shareFilePath?: string(name='share_file_path', description='share_path', example='/a/b/c/', x-go-name='SharePath'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
    shareName?: string(name='share_name', description='share_name', example='new_share', x-go-name='ShareName'),
    status?: string(name='status', description='status', example='enabled', x-go-name='Status'),
    updatedAt?: string(name='updated_at', description='updated_at', example='2006-01-02T15:04:05.999Z', x-go-name='UpdatedAt'),
  }

  /**
   * 
   */
  model BatchSubResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    body?: object(name='body', additionalProperties='{"type":"object"}', description='body 子请求的返回结果，可参考对于子请求文档 json 字符串', example='{"drive_id":"101","file_id":"xxxxxx"}', x-go-name='Body'),
    id?: string(name='id', description='id 请求带过来的id, 可以跟 request 进行关联', example='"1"', x-go-name='ID'),
    status?: long(name='status', description='status 子请求的返回状态码，可参考对于子请求文档', example=200, format='int64', x-go-name='Status'),
  }

  /**
   * batch operation response
   */
  model CCPBatchResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    responses?: [ BatchSubResponse ](name='responses', description='responses 返回结果合集', items='{"$ref":"#/definitions/BatchSubResponse"}', x-go-name='Responses'),
  }

  /**
   * complete file response
   */
  model CCPCompleteFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    category?: string(name='category', description='category', enum='doc, image, audio, video', example='doc', x-go-name='Category'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='DomainID', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    hidden?: boolean(name='hidden', description='Hidden
type: boolean', enum='false, true', example=false, x-go-name='Hidden'),
    imageMediaMetadata?: [ ImageMediaResponse ](name='image_media_metadata', $ref='#/definitions/ImageMediaResponse'),
    labels?: [ string ](name='labels', description='labels', example='label1:1, label2:2', items='{"type":"string"}', x-go-name='Labels'),
    meta?: string(name='meta', description='meta', example='file meta', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFileId?: string(name='parent_file_id', description='parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFileID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    starred?: boolean(name='starred', description='starred
type: boolean', example=false, x-go-name='Starred'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
    crc?: string(name='crc', description='crc', x-go-name='CRC'),
  }

  /**
   * 文件拷贝 response
   */
  model CCPCopyFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    domainId?: string(name='domain_id', description='DomainID', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
  }

  /**
   * Create file response
   */
  model CCPCreateFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    domainId?: string(name='domain_id', description='domain_id', example='domain', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId?: string(name='file_id', description='file_id', example='5d79206586bb5dd69fb34c349282718146c55da7', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    parentFileId?: string(name='parent_file_id', description='parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFileID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', example='[', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    rapidUpload?: boolean(name='rapid_upload', description='rapid_upload
type: boolean', example=false, x-go-name='RapidUpload'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
  }

  /**
   * 删除文件 response
   */
  model CCPDeleteFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
  }

  /**
   * 批量删除文件 response
   */
  model CCPDeleteFilesResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    deletedFileIdList?: [ string ](name='deleted_file_id_list', description='deleted_file_id_list', example='', items='{"type":"string"}', x-go-name='DeletedFileIDList'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
  }

  /**
   * Get AsyncTask Response
   */
  model CCPGetAsyncTaskResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id
type:string', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    message?: string(name='message', description='message', example='task is running', x-go-name='Message'),
    state?: string(name='state', description='state', enum='Running, Failed, Succeed', example='success', x-go-name='State'),
  }

  /**
   * 获取download url response
   */
  model CCPGetDownloadUrlResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z07:00', x-go-name='Expiration'),
    method?: string(name='method', description='method', example='GET', x-go-name='HttpMethod'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d79219b0aa9a7c995a94a96993ba3205cd91c5a%2F5d79219bf3261a5d38744da0834ed489b677a27a?Expires=xxxOSSAccessKeyId=xxx&Signature=xxx&response-content-disposition=attachment%3Bfilename%3DtBiZAoJPC2c8b13450eda4292b7f5f8010618e078.txt', x-go-name='Url'),
  }

  /**
   * 获取文件元数据response
   */
  model CCPGetFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    category?: string(name='category', description='category', enum='doc, image, audio, video', example='doc', x-go-name='Category'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='DomainID', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    hidden?: boolean(name='hidden', description='Hidden
type: boolean', enum='false, true', example=false, x-go-name='Hidden'),
    imageMediaMetadata?: [ ImageMediaResponse ](name='image_media_metadata', $ref='#/definitions/ImageMediaResponse'),
    labels?: [ string ](name='labels', description='labels', example='label1:1, label2:2', items='{"type":"string"}', x-go-name='Labels'),
    meta?: string(name='meta', description='meta', example='file meta', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFileId?: string(name='parent_file_id', description='parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFileID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    starred?: boolean(name='starred', description='starred
type: boolean', example=false, x-go-name='Starred'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
  }

  /**
   * Get UploadUrl Response
   */
  model CCPGetUploadUrlResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    createAt?: string(name='create_at', description='created_at', example='2019-09-11T16:34:36.977Z', x-go-name='CreateAt'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    uploadId?: string(name='upload_id', description='upload_id', example='F3C25CDFA5C74ECB8DE32672F6211FD4', x-go-name='UploadID'),
  }

  /**
   * List file response
   */
  model CCPListFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseCCPFileResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/BaseCCPFileResponse"}', maxItems=100, x-go-name='FileList'),
    nextMarker?: string(name='next_marker', description='next_marker', example='NWQ1YmI4MjA0ZGU1ZWNjYzAzODM0ZDVkODlkMWJiMzcyNzM1NTU4OA', x-go-name='NextMarker'),
  }

  /**
   * 获取签名 response
   */
  model CCPListUploadedPartResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    nextPartNumberMarker?: string(name='next_part_number_marker', description='next_part_number_marker', example='4', x-go-name='NextPartNumberMarker'),
    uploadId?: string(name='upload_id', description='upload_id', example='F3C25CDFA5C74ECB8DE32672F6211FD4', x-go-name='UploadID'),
    uploadedParts?: [ UploadPartInfo ](name='uploaded_parts', description='uploaded_parts', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='UploadedParts'),
  }

  /**
   * 文件移动 response
   */
  model CCPMoveFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    domainId?: string(name='domain_id', description='DomainID', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
  }

  /**
   * search file response
   */
  model CCPSearchFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseCCPFileResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/BaseCCPFileResponse"}', maxItems=100, x-go-name='FileList'),
    nextMarker?: string(name='next_marker', description='next_marker', example='NWQ1YmI4MjA0ZGU1ZWNjYzAzODM0ZDVkODlkMWJiMzcyNzM1NTU4OA', x-go-name='NextMarker'),
  }

  /**
   * 更新文件元数据 response
   */
  model CCPUpdateFileMetaResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    category?: string(name='category', description='category', enum='doc, image, audio, video', example='doc', x-go-name='Category'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='DomainID', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    fileId?: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='FileID'),
    hidden?: boolean(name='hidden', description='Hidden
type: boolean', enum='false, true', example=false, x-go-name='Hidden'),
    imageMediaMetadata?: [ ImageMediaResponse ](name='image_media_metadata', $ref='#/definitions/ImageMediaResponse'),
    labels?: [ string ](name='labels', description='labels', example='label1:1, label2:2', items='{"type":"string"}', x-go-name='Labels'),
    meta?: string(name='meta', description='meta', example='file meta', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFileId?: string(name='parent_file_id', description='parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFileID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    starred?: boolean(name='starred', description='starred
type: boolean', example=false, x-go-name='Starred'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
  }

  /**
   * 
   */
  model CancelLinkRequest {
    temporaryToken: string(name='temporary_token', description='待绑定的临时token，此token只能访问绑定、取消绑定接口', example='ey***s=', x-go-name='TemporaryToken'),
  }

  /**
   * 
   */
  model Captcha {
    captcha: string(name='captcha', description='图片验证码，base64格式', example='', x-go-name='Captcha'),
    captchaFormat: string(name='captcha_format', description='图片格式', example='png', x-go-name='CaptchaFormat'),
    captchaId: string(name='captcha_id', description='图片验证码ID', example='f4690fc255e2fb24c10ede079d3802b4cClRimRh67Q', x-go-name='CaptchaID'),
  }

  /**
   * 
   */
  model ConfirmLinkRequest {
    temporaryToken: string(name='temporary_token', description='待绑定的临时token，此token只能访问绑定、取消绑定接口', example='ey***s=', x-go-name='TemporaryToken'),
  }

  /**
   * 
   */
  model CorsRule {
    allowedHeader?: [ string ](name='allowed_header', description='AllowedHeader', example='h*', items='{"type":"string"}', x-go-name='AllowedHeader'),
    allowedMethod?: [ string ](name='allowed_method', description='AllowedMethod', example='PUT, GET', items='{"type":"string"}', x-go-name='AllowedMethod'),
    allowedOrigin?: [ string ](name='allowed_origin', description='AllowedOrigin', example='*', items='{"type":"string"}', x-go-name='AllowedOrigin'),
    exposeHeader?: [ string ](name='expose_header', description='ExposeHeader', example='ETag', items='{"type":"string"}', x-go-name='ExposeHeader'),
    maxAgeSeconds?: long(name='max_age_seconds', description='MaxAgeSeconds', example=0, format='int64', x-go-name='MaxAgeSeconds'),
  }

  /**
   * 
   */
  model CreateAppRequest {
    appName: string(name='app_name', description='App名称', example='图片分享应用', maxLength=128, minLength=1, x-go-name='AppName'),
    description?: string(name='description', description='App描述', example='测试专用', maxLength=128, minLength=0, x-go-name='Description'),
    isThirdParty?: boolean(name='is_third_party', default=false, description='是否是domain私有App', example=false, x-go-name='IsThirdParty'),
    logo: string(name='logo', description='App图标', example='https://app.com/logo.jpg', x-go-name='Logo'),
    redirectUri?: string(name='redirect_uri', description='App回调地址', example='https://app.com/callback', x-go-name='RedirectUri'),
    scope: [ string ](name='scope', description='App权限列表', enum='FILE.ALL, SHARE.ALL, STORAGEFILE.ALL, STORAGE.ALL, DRIVE.ALL, USER.ALL', items='{"type":"string"}', x-go-name='Scope'),
    type: string(name='type', default='WebServer', description='App类型', enum='WebServer, WebBrowser, Native', example='WebServer', x-go-name='Type'),
  }

  /**
   * create domain request
   */
  model CreateDomainRequest {
    authAlipayAppId?: string(name='auth_alipay_app_id', description='支付宝 App Id', example='abc', x-go-name='AuthAlipayAppId'),
    authAlipayEnbale?: boolean(name='auth_alipay_enbale', description='启用支付宝认证', example=true, x-go-name='AuthAlipayEnable'),
    authAlipayPrivateKey?: string(name='auth_alipay_private_key', description='支付宝 App Secret', example='abc', x-go-name='AuthAlipayPrivateKey'),
    authConfig?: object(name='auth_config', additionalProperties='{"$ref":"#/definitions/AuthConfig"}', description='登录相关信息', example='{}', x-go-name='AuthConfig'),
    authDingdingAppId?: string(name='auth_dingding_app_id', description='钉钉 App Id', example='abc', x-go-name='AuthDingdingAppId'),
    authDingdingAppSecret?: string(name='auth_dingding_app_secret', description='钉钉 App Secret', example='abc', x-go-name='AuthDingdingAppSecret'),
    authDingdingEnable?: boolean(name='auth_dingding_enable', description='启用钉钉认证', example=true, x-go-name='AuthDingdingEnable'),
    authEndpointEnable?: boolean(name='auth_endpoint_enable', x-go-name='AuthEndpointEnable'),
    authRamAppId?: string(name='auth_ram_app_id', description='RAM App Id', example='abc', x-go-name='AuthRamAppId'),
    authRamAppSecret?: string(name='auth_ram_app_secret', description='RAM App Secret', example='abc', x-go-name='AuthRamAppSecret'),
    authRamEnable?: boolean(name='auth_ram_enable', description='启用 RAM 认证', example=true, x-go-name='AuthRamEnable'),
    dataHashName?: string(name='data_hash_name', description='数据 Hash 算法', example='sha1', x-go-name='DataHashName'),
    description?: string(name='description', description='Domain 描述', example='my test domain', x-go-name='Description'),
    domainName: string(name='domain_name', description='Domain 名称', example='test_domain', x-go-name='DomainName'),
    eventFilenameMatches?: string(name='event_filename_matches', description='事件通知 MNS 匹配文件名', example='abc', x-go-name='EventFileNameMatches'),
    eventMnsEndpoint?: string(name='event_mns_endpoint', description='事件通知 MNS Endpoint', example='http://abc.mns.cn-shenzhen.aliyuncs.com', x-go-name='EventMnsEndpoint'),
    eventMnsTopic?: string(name='event_mns_topic', description='事件通知 MNS Topic', example='abc', x-go-name='EventMnsTopic'),
    eventNames?: [ string ](name='event_names', description='事件名列表', example='abc', items='{"type":"string"}', x-go-name='EventNames'),
    eventRoleArn?: string(name='event_role_arn', description='事件通知 Role Arn', x-go-name='EventRoleArn'),
    initDriveEnable?: boolean(name='init_drive_enable', description='开启自动初始化 Drive', example=true, x-go-name='InitDriveEnable'),
    initDriveSize?: long(name='init_drive_size', description='自动初始化 Drive 大小', example=1024, format='int64', x-go-name='InitDriveSize'),
    pathType: string(name='path_type', description='Domain 类型', example='CCPPath', x-go-name='PathType'),
    publishedAppAccessStrategy?: [ AppAccessStrategy ](name='published_app_access_strategy', $ref='#/definitions/AppAccessStrategy'),
    sharable?: boolean(name='sharable', description='开启分享', example=true, x-go-name='Sharable'),
    storeLevel?: string(name='store_level', description='存储级别', example='Standard', x-go-name='StoreLevel'),
    storeRegionList: [ string ](name='store_region_list', description='存储 Region 列表', example='cn-hangzhou', items='{"type":"string"}', x-go-name='StoreRegionList'),
  }

  /**
   * Create drive response
   */
  model CreateDriveResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
  }

  /**
   * Create share response
   */
  model CreateShareResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    domainId?: string(name='domain_id', description='domain_id', example='domain-1', x-go-name='DomainID'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
  }

  /**
   * 
   */
  model DefaultChangePasswordRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    newPassword: string(name='new_password', description='新密码，必须包含数字和字母，长度8-32个字符', example='123456,abc', x-go-name='NewPassword'),
    phoneNumber: string(name='phone_number', description='手机号', example='152*****341', x-go-name='PhoneNumber'),
    smsCode: string(name='sms_code', description='短信验证码内容', example='1234', x-go-name='SmsCode'),
    smsCodeId: string(name='sms_code_id', description='短信验证码ID', example='csjanwia', x-go-name='SmsCodeID'),
  }

  /**
   * 
   */
  model DefaultSetPasswordRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    newPassword: string(name='new_password', description='新密码，必须包含数字和字母，长度8-32个字符', example='123456,abc', x-go-name='NewPassword'),
    state: string(name='state', description='临时操作权限码', example='abc', x-go-name='State'),
  }

  /**
   * 
   */
  model DeleteAppRequest {
    appId: string(name='app_id', description='App ID', example='CNMrhxwPJHAReExa', x-go-name='AppID'),
  }

  /**
   * delete domain request
   */
  model DeleteDomainRequest {
    domainId: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
  }

  /**
   * delete drive response
   */
  model DeleteDriveResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
  }

  /**
   * 
   */
  model GetAccessTokenByLinkInfoRequest {
    identity: string(name='identity', description='唯一身份标识', example='15208333333', x-go-name='Identity'),
    type: string(name='type', description='认证类型', example='mobile', x-go-name='Type'),
  }

  /**
   * 
   */
  model GetAppRequest {
    appId: string(name='app_id', description='App ID', example='CNMrhxwPJHAReExa', x-go-name='AppID'),
  }

  /**
   * 
   */
  model GetByLinkInfoRequest {
    identity: string(name='identity', description='唯一身份标识', example='15208333333', x-go-name='Identity'),
    type: string(name='type', description='认证类型', example='mobile', x-go-name='Type'),
  }

  /**
   * 
   */
  model GetCaptchaRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
  }

  /**
   * get domain request
   */
  model GetDomainRequest {
    domainId: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
  }

  /**
   * Get drive response
   */
  model GetDriveResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    creator?: string(name='creator', description='Drive 创建者', example='ccp', x-go-name='Creator'),
    description?: string(name='description', description='Drive 备注信息', example='ccp team drive', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
    driveName?: string(name='drive_name', description='Drive 名称', example='ccpdrive', x-go-name='DriveName'),
    driveType?: string(name='drive_type', description='Drive 类型', example='normal', x-go-name='DriveType'),
    owner?: string(name='owner', description='Drive 所有者', example='ccp', x-go-name='Owner'),
    relativePath?: string(name='relative_path', description='Drive存储基于store的相对路径，domain的PathType为OSSPath时返回', example='/a/b/e/', x-go-name='RelativePath'),
    status?: string(name='status', description='Drive 状态', example='enabled', x-go-name='Status'),
    storeId?: string(name='store_id', description='存储 ID, domain的PathType为OSSPath时返回', example='123', x-go-name='StoreID'),
    totalSize?: long(name='total_size', description='Drive 空间总量', example=102400, format='int64', x-go-name='TotalSize'),
    usedSize?: long(name='used_size', description='Drive 空间已使用量', example=1024, format='int64', x-go-name='UsedSize'),
  }

  /**
   * 
   */
  model GetLinkInfoByUserIDRequest {
    userId: string(name='user_id', description='user ID', example='123987123617823', x-go-name='UserID'),
  }

  /**
   * Get share response
   */
  model GetShareResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    createdAt?: string(name='created_at', description='created_at', example='2006-01-02T15:04:05.999Z', x-go-name='CreatedAt'),
    creator?: string(name='creator', description='creator', example='xxx', x-go-name='Creator'),
    description?: string(name='description', description='description', example='share description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='domain-1', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', x-go-name='DriveID'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z', x-go-name='ExpiredTime'),
    expired?: boolean(name='expired', description='expired', example=false, x-go-name='Expired'),
    owner?: string(name='owner', description='owner', example='xxx', x-go-name='Owner'),
    permissions?: [ string ](name='permissions', description='permissions', example='', items='{"type":"string"}', x-go-name='Permissions'),
    shareFilePath?: string(name='share_file_path', description='share_path', example='/a/b/c/', x-go-name='SharePath'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
    shareName?: string(name='share_name', description='share_name', example='new_share', x-go-name='ShareName'),
    status?: string(name='status', description='status', example='enabled', x-go-name='Status'),
    updatedAt?: string(name='updated_at', description='updated_at', example='2006-01-02T15:04:05.999Z', x-go-name='UpdatedAt'),
  }

  /**
   * 
   */
  model ImageMediaResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    height?: long(name='height', default=0, description='height', example=720, format='int64', x-go-name='ImageHeight'),
    time?: string(name='time', description='time', format='date-time', x-go-name='ImageTakeAt'),
    width?: long(name='width', default=0, description='width', example=1024, format='int64', x-go-name='ImageWidth'),
  }

  /**
   * 
   */
  model LinkInfo {
    identity?: string(name='identity', description='当前用户已存在的登录标识', example='15208345000', x-go-name='Identity'),
    type?: string(name='type', description='当前用户已存在的登录方式', example='mobile', x-go-name='Type'),
  }

  /**
   * 
   */
  model LinkInfoListResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items: [ LinkInfoResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/LinkInfoResponse"}', x-go-name='Items'),
  }

  /**
   * 
   */
  model LinkInfoResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    authenticationType: string(name='authentication_type', description='认证类型', example='mobile', x-go-name='AuthenticationType'),
    createdAt: long(name='created_at', description='创建时间', example=1556163159820, format='int64', x-go-name='CreatedAt'),
    domainId: string(name='domain_id', description='Domain ID', example='5000', x-go-name='DomainID'),
    identity: string(name='identity', description='唯一身份标识', example='15208345000', x-go-name='Identity'),
    lastLoginTime: long(name='last_login_time', description='最后登录时间', example=1556163159820, format='int64', x-go-name='LastLoginTime'),
    status: string(name='status', description='状态', example='normal', x-go-name='Status'),
    userId: string(name='user_id', description='用户ID', example='00016a587b62b50003deea299a4f5b50', x-go-name='UserID'),
  }

  /**
   * 
   */
  model ListAppsRequest {
    limit?: integer(name='limit', default=50, description='返回结果数据', example=100, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='下次查询游标', example='ncsajnsalsa=', x-go-name='Marker'),
  }

  /**
   * list domain request
   */
  model ListDomainsRequest {
    limit?: integer(name='limit', description='分页大小', example=100, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='查询游标', example='abcd', x-go-name='Marker'),
  }

  /**
   * list drive response
   */
  model ListDriveResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseDriveResponse ](name='items', description='Drive 列表', items='{"$ref":"#/definitions/BaseDriveResponse"}', x-go-name='DriveList'),
    nextMarker?: string(name='next_marker', description='翻页标记', example='marker', x-go-name='NextMarker'),
  }

  /**
   * List share response
   */
  model ListShareResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseShareResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/BaseShareResponse"}', x-go-name='ShareList'),
    nextMarker?: string(name='next_marker', description='next_marker', x-go-name='NextMarker'),
  }

  /**
   * List storage file
   */
  model ListStoreFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ StoreFile ](name='items', description='items
file list', example='', items='{"$ref":"#/definitions/StoreFile"}', x-go-name='FileList'),
    nextMarker?: string(name='next_marker', x-go-name='NextMarker'),
  }

  /**
   * List storage
   */
  model ListStoreResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ StoreItemResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/StoreItemResponse"}', x-go-name='StoreList'),
  }

  /**
   * 
   */
  model ListStoresRequest {
    domainId: string(name='domain_id', description='domain ID', example='sz100', x-go-name='DomainID'),
  }

  /**
   * 
   */
  model LoginByCodeRequest {
    accessToken?: string(name='access_token', description='鉴权后返回的accessToken，淘宝登录需要此字段', example='csaklidwasdhjwid', x-go-name='AccessToken'),
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    authCode?: string(name='auth_code', description='鉴权后返回的AuthCode，支付宝登录需要此字段', example='csaklidwasdhjwid', x-go-name='AuthCode'),
    type: string(name='type', description='鉴权类型，淘宝、支付宝', enum='taobao, alipay', example='taobao', x-go-name='Type'),
  }

  /**
   * 
   */
  model MobileCheckExistRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    phoneNumber: string(name='phone_number', description='待查询的手机号', example='152*****341', x-go-name='PhoneNumber'),
  }

  /**
   * 
   */
  model MobileCheckExistResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    isExist: boolean(name='is_exist', description='当前手机号是否存在', example=true, x-go-name='IsExist'),
    phoneNumber: string(name='phone_number', description='待查询的手机号', example='152*****341', x-go-name='PhoneNumber'),
  }

  /**
   * 
   */
  model MobileLoginRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    autoRegister?: boolean(name='auto_register', default=false, description='是否自动注册用户，使用密码登录此参数不生效', example=false, x-go-name='AutoRegister'),
    captchaId?: string(name='captcha_id', description='图片验证码ID, 密码登录需要此参数', example='abc', x-go-name='CaptchaID'),
    captchaText?: string(name='captcha_text', description='用户输入的验证码值, 密码登录需要此参数', example='abc', x-go-name='CaptchaText'),
    password?: string(name='password', description='登录密码, 传入此参数则忽略短信验证码，不传此参数则默认使用短信登录。', example='123456', x-go-name='Password'),
    phoneNumber: string(name='phone_number', description='待查询的手机号', example='152*****341', x-go-name='PhoneNumber'),
    smsCode?: string(name='sms_code', description='短信验证码内容，使用密码登录此参数不生效', example='1234', x-go-name='SmsCode'),
    smsCodeId?: string(name='sms_code_id', description='短信验证码ID，使用密码登录此参数不生效', example='csjanwia', x-go-name='SmsCodeID'),
  }

  /**
   * 
   */
  model MobileRegisterRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    phoneNumber: string(name='phone_number', description='待查询的手机号', example='152*****341', x-go-name='PhoneNumber'),
    smsCode: string(name='sms_code', description='短信验证码内容', example='1234', x-go-name='SmsCode'),
    smsCodeId: string(name='sms_code_id', description='短信验证码ID', example='csjanwia', x-go-name='SmsCodeID'),
  }

  /**
   * 
   */
  model MobileSendSmsCodeRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    captchaId?: string(name='captcha_id', description='图片验证码ID', example='abc', x-go-name='CaptchaID'),
    captchaText?: string(name='captcha_text', description='用户输入的验证码值', example='abc', x-go-name='CaptchaText'),
    phoneNumber: string(name='phone_number', description='待发送验证短信的手机号', example='152*****341', x-go-name='PhoneNumber'),
    type: string(name='type', description='验证码用途', example='abc', x-go-name='Type'),
  }

  /**
   * 
   */
  model MobileSendSmsCodeResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    smsCodeId: string(name='sms_code_id', description='短信验证码ID', example='csjanwia', x-go-name='SmsCodeID'),
  }

  /**
   * complete file response
   */
  model OSSCompleteFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    filePath?: string(name='file_path', description='file_path', example='/a.jpg', x-go-name='FilePath'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFilePath?: string(name='parent_file_path', description='parent_file_id', example='/', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFilePath'),
    shareId?: string(name='share_id', description='share_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', pattern='[0-9]+', x-go-name='ShareID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
    crc?: string(name='crc', description='crc', x-go-name='CRC'),
  }

  /**
   * 文件拷贝 response
   */
  model OSSCopyFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z-]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    shareId?: string(name='share_id', description='drive_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='ShareID'),
  }

  /**
   * Create file response
   */
  model OSSCreateFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    domainId?: string(name='domain_id', description='domain_id', example='domain', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', example='[', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    shareId?: string(name='share_id', description='share_id', example='1', pattern='[0-9]+', x-go-name='ShareID'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
  }

  /**
   * 删除文件 response
   */
  model OSSDeleteFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    shareId?: string(name='share_id', description='share_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', pattern='[a-z0-9A-Z]+', x-go-name='ShareID'),
  }

  /**
   * 批量删除文件 response
   */
  model OSSDeleteFilesResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    deletedFileIdList?: [ string ](name='deleted_file_id_list', description='deleted_file_id_list', example='', items='{"type":"string"}', x-go-name='DeletedFileIDList'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    shareId?: string(name='share_id', description='share_id', example='1', pattern='[0-9]+', x-go-name='ShareID'),
  }

  /**
   * 获取download url response
   */
  model OSSGetDownloadUrlResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z07:00', x-go-name='Expiration'),
    method?: string(name='method', description='method', example='GET', x-go-name='HttpMethod'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d79219b0aa9a7c995a94a96993ba3205cd91c5a%2F5d79219bf3261a5d38744da0834ed489b677a27a?Expires=xxxOSSAccessKeyId=xxx&Signature=xxx&response-content-disposition=attachment%3Bfilename%3DtBiZAoJPC2c8b13450eda4292b7f5f8010618e078.txt', x-go-name='Url'),
  }

  /**
   * 获取文件元数据response
   */
  model OSSGetFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    filePath?: string(name='file_path', description='file_path', example='/a.jpg', x-go-name='FilePath'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFilePath?: string(name='parent_file_path', description='parent_file_id', example='/', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFilePath'),
    shareId?: string(name='share_id', description='share_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', pattern='[0-9]+', x-go-name='ShareID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
  }

  /**
   * Get UploadUrl Response
   */
  model OSSGetUploadUrlResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    createAt?: string(name='create_at', description='created_at', example='2019-09-11T16:34:36.977Z', x-go-name='CreateAt'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    uploadId?: string(name='upload_id', description='upload_id', example='F3C25CDFA5C74ECB8DE32672F6211FD4', x-go-name='UploadID'),
  }

  /**
   * List file response
   */
  model OSSListFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseOSSFileResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/BaseOSSFileResponse"}', maxItems=100, x-go-name='FileList'),
    nextMarker?: string(name='next_marker', description='next_marker', example='NWQ1YmI4MjA0ZGU1ZWNjYzAzODM0ZDVkODlkMWJiMzcyNzM1NTU4OA', x-go-name='NextMarker'),
  }

  /**
   * 获取签名 response
   */
  model OSSListUploadedPartResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    nextPartNumberMarker?: string(name='next_part_number_marker', description='next_part_number_marker', example='4', x-go-name='NextPartNumberMarker'),
    uploadId?: string(name='upload_id', description='upload_id', example='F3C25CDFA5C74ECB8DE32672F6211FD4', x-go-name='UploadID'),
    uploadedParts?: [ UploadPartInfo ](name='uploaded_parts', description='uploaded_parts', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='UploadedParts'),
  }

  /**
   * 文件移动 response
   */
  model OSSMoveFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    asyncTaskId?: string(name='async_task_id', description='async_task_id', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z-]+', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    shareId?: string(name='share_id', description='drive_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='ShareID'),
  }

  /**
   * search file response
   */
  model OSSSearchFileResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseOSSFileResponse ](name='items', description='items', example='', items='{"$ref":"#/definitions/BaseOSSFileResponse"}', maxItems=100, x-go-name='FileList'),
    nextMarker?: string(name='next_marker', description='next_marker', example='NWQ1YmI4MjA0ZGU1ZWNjYzAzODM0ZDVkODlkMWJiMzcyNzM1NTU4OA', x-go-name='NextMarker'),
  }

  /**
   * 更新文件元数据 response
   */
  model OSSUpdateFileMetaResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    contentHash?: string(name='content_hash', description='Content Hash', example='EA4942AA8761213890A5C386F88E6464D2C31CA3', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentType?: string(name='content_type', description='content_type', example='application/json', x-go-name='ContentType'),
    crc64Hash?: string(name='crc64_hash', description='crc64_hash', x-go-name='CRC'),
    createdAt?: string(name='created_at', description='created_at', example='2019-08-20T06:51:27.292Z', x-go-name='CreatedAt'),
    description?: string(name='description', description='description', example='file description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='test001', pattern='[a-z0-9A-Z]+', x-go-name='DomainID'),
    downloadUrl?: string(name='download_url', description='download_url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='DownloadUrl'),
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileExtension?: string(name='file_extension', description='file_extension', example='jpg', x-go-name='FileExtension'),
    filePath?: string(name='file_path', description='file_path', example='/a.jpg', x-go-name='FilePath'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFilePath?: string(name='parent_file_path', description='parent_file_id', example='/', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFilePath'),
    shareId?: string(name='share_id', description='share_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', pattern='[0-9]+', x-go-name='ShareID'),
    size?: long(name='size', description='Size', example=1024, format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    status?: string(name='status', description='status', enum='uploading, available', example='available', x-go-name='Status'),
    thumbnail?: string(name='thumbnail', description='thumbnail', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='ThumbnailUrl'),
    trashedAt?: string(name='trashed_at', description='trashed_at', format='date-time', x-go-name='TrashedAt'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
    updatedAt?: string(name='updated_at', description='updated_at', format='date-time', x-go-name='UpdatedAt'),
    uploadId?: string(name='upload_id', description='upload_id', example='C9DCFE5A82644AC7A02DB74C30C934A6', x-go-name='UploadID'),
    url?: string(name='url', description='url', example='https://ccp.data.aliyuncs.com/hz22%2F5d5b986facbec311ef844c25954f96821497b383%2F5d5b986f955410dd991646bb87c6b4e899eff525?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx', x-go-name='Url'),
  }

  /**
   * Pre hash check Response
   */
  model PreHashCheckSuccessResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    code?: string(name='code', description='code', x-go-name='Code'),
    fileName?: string(name='file_name', description='file_name', x-go-name='FileName'),
    message?: string(name='message', description='message', x-go-name='Message'),
    parentFileId: string(name='parent_file_id', description='parent_file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1, 50}', x-go-name='ParentFileId'),
    preHash?: string(name='pre_hash', description='pre_hash', x-go-name='PreHash'),
  }

  /**
   * 
   */
  model RemoveStoreRequest {
    domainId: string(name='domain_id', description='domain ID', example='sz100', x-go-name='DomainID'),
    storeId: string(name='store_id', description='store ID', example='cdjaksbwuiqbxuiassd', x-go-name='StoreID'),
  }

  /**
   * 
   */
  model SetStoreCorsRequest {
    corsRule: [ CorsRule ](name='cors_rule', $ref='#/definitions/CorsRule'),
    domainId: string(name='domain_id', description='domain ID', example='sz100', x-go-name='DomainID'),
    storeId: string(name='store_id', description='store ID', example='cdjaksbwuiqbxuiassd', x-go-name='StoreID'),
  }

  /**
   * 
   */
  model StoreFile {
    domainId?: string(name='domain_id', x-go-name='DomainId'),
    name?: string(name='name', x-go-name='Name'),
    parentFilePath?: string(name='parent_file_path', x-go-name='ParentFilePath'),
    storeId?: string(name='store_id', x-go-name='StoreID'),
    type?: string(name='type', x-go-name='Type'),
  }

  /**
   * 
   */
  model StoreItemResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    accelerateEndpoint?: string(name='accelerate_endpoint', description='全球加速域名', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='AccelerateEndpoint'),
    basePath?: string(name='base_path', description='存储公共前缀', example='ccp-data', x-go-name='BasePath'),
    bucket: string(name='bucket', description='bucket名称', example='data-sz-bucket', x-go-name='Bucket'),
    customizedEndpoint?: string(name='customized_endpoint', description='用户自定义绑定存储地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='CustomizedEndpoint'),
    domainId?: string(name='domain_id', x-go-name='DomainId'),
    endpoint: string(name='endpoint', description='存储访问地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='Endpoint'),
    internalEndpoint?: string(name='internal_endpoint', description='内网存储地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='InternalEndpoint'),
    ownership: string(name='ownership', description='存储归属，system表示系统提供，custom表示使用自己的存储', enum='system, custom', example='system', x-go-name='Ownership'),
    policy: string(name='policy', description='Policy授权,system类型store会将bucket权限授予当前云账号', x-go-name='Policy'),
    roleArn?: string(name='role_arn', description='访问Bucket的角色ARN', example='acs:oss:*:*:*', x-go-name='RoleArn'),
    storeId: string(name='store_id', description='store ID', example='cdjaksbwuiqbxuiassd', x-go-name='StoreID'),
    type: string(name='type', description='存储类型，当前只支持oss', enum='oss', example='oss', x-go-name='Type'),
  }

  /**
   * 
   */
  model TokenRequest {
    appId: string(name='app_id', description='App ID, 当前访问的App', example='csaklidwasdhjwid', x-go-name='AppID'),
    grantType: string(name='grant_type', description='只能填refresh_token', example='refresh_token', x-go-name='GrantType'),
    refreshToken: string(name='refresh_token', description='refresh token, 登录时返回的', example='ybb3WJy2CwXHoM6hBcydGlvzMoJkFpkk', x-go-name='RefreshToken'),
  }

  /**
   * 
   */
  model UpdateAppRequest {
    appId: string(name='app_id', description='App ID', example='CNMrhxwPJHAReExa', x-go-name='AppID'),
    appName: string(name='app_name', description='App名称', example='图片分享应用', maxLength=128, minLength=1, pattern='[0-9a-zA-Z]+', x-go-name='AppName'),
    description?: string(name='description', description='App描述', example='测试专用', maxLength=128, minLength=0, x-go-name='Description'),
    isThirdParty: boolean(name='is_third_party', description='是否是domain私有App', example=false, x-go-name='IsThirdParty'),
    logo: string(name='logo', description='App图标', example='https://app.com/logo.jpg', x-go-name='Logo'),
    redirectUri: string(name='redirect_uri', description='App回调地址', example='https://app.com/callback', x-go-name='RedirectUri'),
    scope: [ string ](name='scope', description='App权限列表', enum='FILE.ALL, SHARE.ALL, STORAGEFILE.ALL, STORAGE.ALL, DRIVE.ALL, USER.ALL', items='{"type":"string"}', x-go-name='Scope'),
    type: string(name='type', default='WebServer', description='App类型', enum='WebServer, WebBrowser, Native', example='WebServer', x-go-name='Type'),
  }

  /**
   * update domain request
   */
  model UpdateDomainRequest {
    authAlipayAppId?: string(name='auth_alipay_app_id', description='支付宝 App Id', example='abc', x-go-name='AuthAlipayAppId'),
    authAlipayEnbale?: boolean(name='auth_alipay_enbale', description='启用支付宝认证', example=true, x-go-name='AuthAlipayEnable'),
    authAlipayPrivateKey?: string(name='auth_alipay_private_key', description='支付宝 App Secret', example='abc', x-go-name='AuthAlipayPrivateKey'),
    authConfig?: object(name='auth_config', additionalProperties='{"$ref":"#/definitions/AuthConfig"}', x-go-name='AuthConfig'),
    authDingdingAppId?: string(name='auth_dingding_app_id', description='钉钉 App Id', example='abc', x-go-name='AuthDingdingAppId'),
    authDingdingAppSecret?: string(name='auth_dingding_app_secret', description='钉钉 App Secret', example='abc', x-go-name='AuthDingdingAppSecret'),
    authDingdingEnable?: boolean(name='auth_dingding_enable', description='启用钉钉认证', example=true, x-go-name='AuthDingdingEnable'),
    authEndpointEnable?: boolean(name='auth_endpoint_enable', x-go-name='AuthEndpointEnable'),
    authRamAppId?: string(name='auth_ram_app_id', description='RAM App Id', example='abc', x-go-name='AuthRamAppId'),
    authRamAppSecret?: string(name='auth_ram_app_secret', description='RAM App Secret', example='abc', x-go-name='AuthRamAppSecret'),
    authRamEnable?: boolean(name='auth_ram_enable', description='启用 RAM 认证', example=true, x-go-name='AuthRamEnable'),
    dataHashName?: string(name='data_hash_name', description='数据 Hash 算法', example='sha1', x-go-name='DataHashName'),
    description?: string(name='description', description='Domain 描述', example='my test domain', x-go-name='Description'),
    domainId: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
    domainName?: string(name='domain_name', description='Domain 名称', example='test_domain', x-go-name='DomainName'),
    eventFilenameMatches?: string(name='event_filename_matches', description='事件通知 MNS 匹配文件名', example='abc', x-go-name='EventFileNameMatches'),
    eventMnsEndpoint?: string(name='event_mns_endpoint', description='事件通知 MNS Endpoint', example='http://abc.mns.cn-shenzhen.aliyuncs.com', x-go-name='EventMnsEndpoint'),
    eventMnsTopic?: string(name='event_mns_topic', description='事件通知 MNS Topic', example='abc', x-go-name='EventMnsTopic'),
    eventNames?: [ string ](name='event_names', description='事件名列表', example='abc', items='{"type":"string"}', x-go-name='EventNames'),
    eventRoleArn?: string(name='event_role_arn', description='事件通知 Role Arn', x-go-name='EventRoleArn'),
    initDriveEnable?: boolean(name='init_drive_enable', description='开启自动初始化 Drive', example=true, x-go-name='InitDriveEnable'),
    initDriveSize?: long(name='init_drive_size', description='自动初始化 Drive 大小', example=1024, format='int64', x-go-name='InitDriveSize'),
    initDriveStoreId?: string(name='init_drive_store_id', description='自动初始化 Drive 使用 Store ID', example='1', x-go-name='InitDriveStoreID'),
    publishedAppAccessStrategy?: [ AppAccessStrategy ](name='published_app_access_strategy', $ref='#/definitions/AppAccessStrategy'),
    sharable?: boolean(name='sharable', description='开启分享', example=true, x-go-name='Sharable'),
  }

  /**
   * Update drive response
   */
  model UpdateDriveResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    creator?: string(name='creator', description='Drive 创建者', example='ccp', x-go-name='Creator'),
    description?: string(name='description', description='Drive 备注信息', example='ccp team drive', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
    driveName?: string(name='drive_name', description='Drive 名称', example='ccpdrive', x-go-name='DriveName'),
    driveType?: string(name='drive_type', description='Drive 类型', example='normal', x-go-name='DriveType'),
    owner?: string(name='owner', description='Drive 所有者', example='ccp', x-go-name='Owner'),
    relativePath?: string(name='relative_path', description='Drive存储基于store的相对路径，domain的PathType为OSSPath时返回', example='/a/b/e/', x-go-name='RelativePath'),
    status?: string(name='status', description='Drive 状态', example='enabled', x-go-name='Status'),
    storeId?: string(name='store_id', description='存储 ID, domain的PathType为OSSPath时返回', example='123', x-go-name='StoreID'),
    totalSize?: long(name='total_size', description='Drive 空间总量', example=102400, format='int64', x-go-name='TotalSize'),
    usedSize?: long(name='used_size', description='Drive 空间已使用量', example=1024, format='int64', x-go-name='UsedSize'),
  }

  /**
   * Update share response
   */
  model UpdateShareResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    createdAt?: string(name='created_at', description='created_at', example='2006-01-02T15:04:05.999Z', x-go-name='CreatedAt'),
    creator?: string(name='creator', description='creator', example='xxx', x-go-name='Creator'),
    description?: string(name='description', description='description', example='share description', x-go-name='Description'),
    domainId?: string(name='domain_id', description='domain_id', example='domain-1', x-go-name='DomainID'),
    driveId?: string(name='drive_id', description='drive_id', example='1', x-go-name='DriveID'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z', x-go-name='ExpiredTime'),
    expired?: boolean(name='expired', description='expired', example=false, x-go-name='Expired'),
    owner?: string(name='owner', description='owner', example='xxx', x-go-name='Owner'),
    permissions?: [ string ](name='permissions', description='permissions', example='', items='{"type":"string"}', x-go-name='Permissions'),
    shareFilePath?: string(name='share_file_path', description='share_path', example='/a/b/c/', x-go-name='SharePath'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
    shareName?: string(name='share_name', description='share_name', example='new_share', x-go-name='ShareName'),
    status?: string(name='status', description='status', example='enabled', x-go-name='Status'),
    updatedAt?: string(name='updated_at', description='updated_at', example='2006-01-02T15:04:05.999Z', x-go-name='UpdatedAt'),
  }

  /**
   * 
   */
  model UploadPartInfo {
    etag?: string(name='etag', description='etag', example='0CC175B9C0F1B6A831C399E269772661', x-go-name='Etag'),
    partNumber?: long(name='part_number', description='PartNumber', example=1, format='int64', maximum=1, minimum=10000, pattern='[0-9]+', x-go-name='PartNumber'),
    partSize?: long(name='part_size', description='PartSize：', example=1024, format='int64', maximum=5368709120, minimum=102400, x-go-name='PartSize'),
    uploadUrl?: string(name='upload_url', description='upload_url', example='https://ccp.data.aliyuncs.com/xxx/xxx?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx&partNumber=1&uploadId=0CC175B9C0F1B6A831C399E269772661', x-go-name='UploadUrl'),
  }

  /**
   * 
   */
  model UserAuthentication {
    AuthenticationType: string(name='AuthenticationType', description='认证类型', example='mobile'),
    CreatedAt: long(name='CreatedAt', description='创建时间', example=1556163159820, format='int64'),
    Detail: string(name='Detail', description='详情', example='{name:abc}'),
    DomainID: string(name='DomainID', description='Domain ID', example='5000'),
    Identity: string(name='Identity', description='唯一身份标识', example='15208345000'),
    LastLoginTime: long(name='LastLoginTime', description='最后登录时间', example=1556163159820, format='int64'),
    Status: string(name='Status', description='状态', example='normal'),
    UserID: string(name='UserID', description='用户ID', example='00016a587b62b50003deea299a4f5b50'),
  }

  /**
   * 
   */
  model AccessTokenResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    accessToken: string(name='access_token', description='用于调用业务接口的accessToken', example='cnskjajkahwdhwialsnd', x-go-name='AccessToken'),
    expiresTime?: string(name='expires_time', description='accessToken过期时间，ISO时间', example='2019-09-01T06:57:48.813Z', x-go-name='ExpiresTime'),
    refreshToken?: string(name='refresh_token', description='用于刷新accessToken', example='cnskjajkahwdhwialsnd', x-go-name='RefreshToken'),
    role?: string(name='role', description='当前用户角色', example='user', x-go-name='Role'),
    tokenType?: string(name='token_type', description='accessToken类型，Bearer', example='Bearer', x-go-name='TokenType'),
    userId?: string(name='user_id', description='当前用户ID', example='DING-xxxxx', x-go-name='UserID'),
  }

  /**
   * 
   */
  model AddStoreResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    accelerateEndpoint?: string(name='accelerate_endpoint', description='全球加速域名', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='AccelerateEndpoint'),
    basePath?: string(name='base_path', description='存储公共前缀', example='ccp-data', x-go-name='BasePath'),
    bucket: string(name='bucket', description='bucket名称', example='data-sz-bucket', x-go-name='Bucket'),
    customizedEndpoint?: string(name='customized_endpoint', description='用户自定义绑定存储地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='CustomizedEndpoint'),
    domainId: string(name='domain_id', description='domain ID', example='sz100', x-go-name='DomainID'),
    endpoint: string(name='endpoint', description='存储访问地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='Endpoint'),
    internalEndpoint?: string(name='internal_endpoint', description='内网存储地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='InternalEndpoint'),
    ownership: string(name='ownership', description='存储归属，system表示系统提供，custom表示使用自己的存储', enum='system, custom', example='system', x-go-name='Ownership'),
    policy: string(name='policy', description='Policy授权,system类型store会将bucket权限授予当前云账号', x-go-name='Policy'),
    roleArn?: string(name='role_arn', description='访问Bucket的角色ARN', example='acs:oss:*:*:*', x-go-name='RoleArn'),
    storeId: string(name='store_id', description='store ID', example='cdjaksbwuiqbxuiassd', x-go-name='StoreID'),
    type: string(name='type', description='存储类型，当前只支持oss', enum='oss', example='oss', x-go-name='Type'),
  }

  /**
   * 
   */
  model AdminListStoresRequest {
  }

  /**
   * base domain response
   */
  model BaseDomainResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    authAlipayAppId?: string(name='auth_alipay_app_id', description='支付宝 App Id', example='abc', x-go-name='AuthAlipayAppId'),
    authAlipayEnable?: boolean(name='auth_alipay_enable', description='是否开启了支付宝认证', example=true, x-go-name='AuthAlipayEnable'),
    authAlipayPrivateKey?: string(name='auth_alipay_private_key', description='支付宝 App Secret', example='abc', x-go-name='AuthAlipayPrivateKey'),
    authConfig?: object(name='auth_config', additionalProperties='{"$ref":"#/definitions/AuthConfig"}', description='登录相关信息', example='{}', x-go-name='AuthConfig'),
    authDingdingAppId?: string(name='auth_dingding_app_id', description='钉钉 App Id', example='abc', x-go-name='AuthDingdingAppId'),
    authDingdingAppSecret?: string(name='auth_dingding_app_secret', description='钉钉 App Secret', example='abc', x-go-name='AuthDingdingAppSecret'),
    authDingdingEnable?: boolean(name='auth_dingding_enable', description='是否开启了钉钉认证', example=true, x-go-name='AuthDingdingEnable'),
    authEndpointEnable?: boolean(name='auth_endpoint_enable', x-go-name='AuthEndpointEnable'),
    authRamAppId?: string(name='auth_ram_app_id', description='RAM App Id', example='abc', x-go-name='AuthRamAppId'),
    authRamAppSecret?: string(name='auth_ram_app_secret', description='RAM App Secret', example='abc', x-go-name='AuthRamAppSecret'),
    authRamEnable?: boolean(name='auth_ram_enable', description='是否开启了 RAM 认证', example=true, x-go-name='AuthRamEnable'),
    createdAt?: string(name='created_at', description='Domain 创建时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='CreatedAt'),
    dataHashName?: string(name='data_hash_name', description='数据 Hash 算法', example='sha1', x-go-name='DataHashName'),
    description?: string(name='description', description='Domain 描述', example='my test domain', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
    domainName?: string(name='domain_name', description='Domain 描述', example='my test domain', x-go-name='DomainName'),
    eventFilenameMatches?: string(name='event_filename_matches', description='事件通知 MNS 匹配文件名', example='abc', x-go-name='EventFileNameMatches'),
    eventMnsEndpoint?: string(name='event_mns_endpoint', description='事件通知 MNS Endpoint', example='http://abc.mns.cn-shenzhen.aliyuncs.com', x-go-name='EventMnsEndpoint'),
    eventMnsTopic?: string(name='event_mns_topic', description='事件通知 MNS Topic', example='abc', x-go-name='EventMnsTopic'),
    eventNames?: [ string ](name='event_names', description='事件名列表', example='abc', items='{"type":"string"}', x-go-name='EventNames'),
    eventRoleArn?: string(name='event_role_arn', description='事件通知 Role Arn', x-go-name='EventRoleArn'),
    initDriveEnable?: boolean(name='init_drive_enable', description='是否开启了自动初始化 Drive', example=true, x-go-name='InitDriveEnable'),
    initDriveSize?: long(name='init_drive_size', description='自动初始化 Drive 大小', example=1024, format='int64', x-go-name='InitDriveSize'),
    initDriveStoreId?: string(name='init_drive_store_id', description='自动初始化 Drive 所用 Store ID', example='1', x-go-name='InitDriveStoreID'),
    pathType?: string(name='path_type', description='Domain 类型', example='CCPPath', x-go-name='PathType'),
    publishedAppAccessStrategy?: [ AppAccessStrategy ](name='published_app_access_strategy', $ref='#/definitions/AppAccessStrategy'),
    sharable?: boolean(name='sharable', description='是否开启了分享', example=true, x-go-name='Sharable'),
    storeLevel?: string(name='store_level', description='存储级别', example='Standard', x-go-name='StoreLevel'),
    storeRegionList?: [ string ](name='store_region_list', description='存储 Region 列表', example='cn-hangzhou', items='{"type":"string"}', x-go-name='StoreRegionList'),
    updatedAt?: string(name='updated_at', description='Domain 更新时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='UpdatedAt'),
  }

  /**
   * create domain response
   */
  model CreateDomainResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    authAlipayAppId?: string(name='auth_alipay_app_id', description='支付宝 App Id', example='abc', x-go-name='AuthAlipayAppId'),
    authAlipayEnable?: boolean(name='auth_alipay_enable', description='是否开启了支付宝认证', example=true, x-go-name='AuthAlipayEnable'),
    authAlipayPrivateKey?: string(name='auth_alipay_private_key', description='支付宝 App Secret', example='abc', x-go-name='AuthAlipayPrivateKey'),
    authConfig?: object(name='auth_config', additionalProperties='{"$ref":"#/definitions/AuthConfig"}', description='登录相关信息', example='{}', x-go-name='AuthConfig'),
    authDingdingAppId?: string(name='auth_dingding_app_id', description='钉钉 App Id', example='abc', x-go-name='AuthDingdingAppId'),
    authDingdingAppSecret?: string(name='auth_dingding_app_secret', description='钉钉 App Secret', example='abc', x-go-name='AuthDingdingAppSecret'),
    authDingdingEnable?: boolean(name='auth_dingding_enable', description='是否开启了钉钉认证', example=true, x-go-name='AuthDingdingEnable'),
    authEndpointEnable?: boolean(name='auth_endpoint_enable', x-go-name='AuthEndpointEnable'),
    authRamAppId?: string(name='auth_ram_app_id', description='RAM App Id', example='abc', x-go-name='AuthRamAppId'),
    authRamAppSecret?: string(name='auth_ram_app_secret', description='RAM App Secret', example='abc', x-go-name='AuthRamAppSecret'),
    authRamEnable?: boolean(name='auth_ram_enable', description='是否开启了 RAM 认证', example=true, x-go-name='AuthRamEnable'),
    createdAt?: string(name='created_at', description='Domain 创建时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='CreatedAt'),
    dataHashName?: string(name='data_hash_name', description='数据 Hash 算法', example='sha1', x-go-name='DataHashName'),
    description?: string(name='description', description='Domain 描述', example='my test domain', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
    domainName?: string(name='domain_name', description='Domain 描述', example='my test domain', x-go-name='DomainName'),
    eventFilenameMatches?: string(name='event_filename_matches', description='事件通知 MNS 匹配文件名', example='abc', x-go-name='EventFileNameMatches'),
    eventMnsEndpoint?: string(name='event_mns_endpoint', description='事件通知 MNS Endpoint', example='http://abc.mns.cn-shenzhen.aliyuncs.com', x-go-name='EventMnsEndpoint'),
    eventMnsTopic?: string(name='event_mns_topic', description='事件通知 MNS Topic', example='abc', x-go-name='EventMnsTopic'),
    eventNames?: [ string ](name='event_names', description='事件名列表', example='abc', items='{"type":"string"}', x-go-name='EventNames'),
    eventRoleArn?: string(name='event_role_arn', description='事件通知 Role Arn', x-go-name='EventRoleArn'),
    initDriveEnable?: boolean(name='init_drive_enable', description='是否开启了自动初始化 Drive', example=true, x-go-name='InitDriveEnable'),
    initDriveSize?: long(name='init_drive_size', description='自动初始化 Drive 大小', example=1024, format='int64', x-go-name='InitDriveSize'),
    initDriveStoreId?: string(name='init_drive_store_id', description='自动初始化 Drive 所用 Store ID', example='1', x-go-name='InitDriveStoreID'),
    pathType?: string(name='path_type', description='Domain 类型', example='CCPPath', x-go-name='PathType'),
    publishedAppAccessStrategy?: [ AppAccessStrategy ](name='published_app_access_strategy', $ref='#/definitions/AppAccessStrategy'),
    sharable?: boolean(name='sharable', description='是否开启了分享', example=true, x-go-name='Sharable'),
    storeLevel?: string(name='store_level', description='存储级别', example='Standard', x-go-name='StoreLevel'),
    storeRegionList?: [ string ](name='store_region_list', description='存储 Region 列表', example='cn-hangzhou', items='{"type":"string"}', x-go-name='StoreRegionList'),
    updatedAt?: string(name='updated_at', description='Domain 更新时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='UpdatedAt'),
  }

  /**
   * 
   */
  model GetAppResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    aliOwnerId: string(name='ali_owner_id', description='App 拥有者', example='172***430', x-go-name='AliOwnerID'),
    appId: string(name='app_id', description='App ID', example='cksaljkfiwasdn', x-go-name='AppID'),
    appName: string(name='app_name', description='App名称', example='图片分享应用', maxLength=128, minLength=1, pattern='[0-9a-zA-Z]+', x-go-name='AppName'),
    appSecret: string(name='app_secret', description='App 秘钥', example='sax***Js9', x-go-name='AppSecret'),
    createdAt: string(name='created_at', description='App 创建时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='CreatedAt'),
    description: string(name='description', description='App描述', example='测试专用', maxLength=128, minLength=0, x-go-name='Description'),
    logo: string(name='logo', description='App图标', example='https://app.com/logo.jpg', x-go-name='Logo'),
    provider: string(name='provider', description='App 提供方', example='Alibaba Inc.', x-go-name='Provider'),
    redirectUri: string(name='redirect_uri', description='App回调地址', example='https://app.com/callback', x-go-name='RedirectUri'),
    scope: [ string ](name='scope', description='App权限列表', enum='FILE.ALL, SHARE.ALL, STORAGEFILE.ALL, STORAGE.ALL, DRIVE.ALL, USER.ALL', items='{"type":"string"}', x-go-name='Scope'),
    screenshots: [ string ](name='screenshots', description='App 屏幕截图', example='http://app.com/screenshot_1.jpg', items='{"type":"string"}', x-go-name='Screenshots'),
    stage: string(name='stage', default='UnPublished,Pending,Published', description='App 当前阶段', example='UnPublished', x-go-name='Stage'),
    type: string(name='type', default='WebServer', description='App类型', enum='WebServer, WebBrowser, Native', example='WebServer', x-go-name='Type'),
    updatedAt: string(name='updated_at', description='App 修改时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='UpdatedAt'),
  }

  /**
   * get domain response
   */
  model GetDomainResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    authAlipayAppId?: string(name='auth_alipay_app_id', description='支付宝 App Id', example='abc', x-go-name='AuthAlipayAppId'),
    authAlipayEnable?: boolean(name='auth_alipay_enable', description='是否开启了支付宝认证', example=true, x-go-name='AuthAlipayEnable'),
    authAlipayPrivateKey?: string(name='auth_alipay_private_key', description='支付宝 App Secret', example='abc', x-go-name='AuthAlipayPrivateKey'),
    authConfig?: object(name='auth_config', additionalProperties='{"$ref":"#/definitions/AuthConfig"}', description='登录相关信息', example='{}', x-go-name='AuthConfig'),
    authDingdingAppId?: string(name='auth_dingding_app_id', description='钉钉 App Id', example='abc', x-go-name='AuthDingdingAppId'),
    authDingdingAppSecret?: string(name='auth_dingding_app_secret', description='钉钉 App Secret', example='abc', x-go-name='AuthDingdingAppSecret'),
    authDingdingEnable?: boolean(name='auth_dingding_enable', description='是否开启了钉钉认证', example=true, x-go-name='AuthDingdingEnable'),
    authEndpointEnable?: boolean(name='auth_endpoint_enable', x-go-name='AuthEndpointEnable'),
    authRamAppId?: string(name='auth_ram_app_id', description='RAM App Id', example='abc', x-go-name='AuthRamAppId'),
    authRamAppSecret?: string(name='auth_ram_app_secret', description='RAM App Secret', example='abc', x-go-name='AuthRamAppSecret'),
    authRamEnable?: boolean(name='auth_ram_enable', description='是否开启了 RAM 认证', example=true, x-go-name='AuthRamEnable'),
    createdAt?: string(name='created_at', description='Domain 创建时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='CreatedAt'),
    dataHashName?: string(name='data_hash_name', description='数据 Hash 算法', example='sha1', x-go-name='DataHashName'),
    description?: string(name='description', description='Domain 描述', example='my test domain', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
    domainName?: string(name='domain_name', description='Domain 描述', example='my test domain', x-go-name='DomainName'),
    eventFilenameMatches?: string(name='event_filename_matches', description='事件通知 MNS 匹配文件名', example='abc', x-go-name='EventFileNameMatches'),
    eventMnsEndpoint?: string(name='event_mns_endpoint', description='事件通知 MNS Endpoint', example='http://abc.mns.cn-shenzhen.aliyuncs.com', x-go-name='EventMnsEndpoint'),
    eventMnsTopic?: string(name='event_mns_topic', description='事件通知 MNS Topic', example='abc', x-go-name='EventMnsTopic'),
    eventNames?: [ string ](name='event_names', description='事件名列表', example='abc', items='{"type":"string"}', x-go-name='EventNames'),
    eventRoleArn?: string(name='event_role_arn', description='事件通知 Role Arn', x-go-name='EventRoleArn'),
    initDriveEnable?: boolean(name='init_drive_enable', description='是否开启了自动初始化 Drive', example=true, x-go-name='InitDriveEnable'),
    initDriveSize?: long(name='init_drive_size', description='自动初始化 Drive 大小', example=1024, format='int64', x-go-name='InitDriveSize'),
    initDriveStoreId?: string(name='init_drive_store_id', description='自动初始化 Drive 所用 Store ID', example='1', x-go-name='InitDriveStoreID'),
    pathType?: string(name='path_type', description='Domain 类型', example='CCPPath', x-go-name='PathType'),
    publishedAppAccessStrategy?: [ AppAccessStrategy ](name='published_app_access_strategy', $ref='#/definitions/AppAccessStrategy'),
    sharable?: boolean(name='sharable', description='是否开启了分享', example=true, x-go-name='Sharable'),
    storeLevel?: string(name='store_level', description='存储级别', example='Standard', x-go-name='StoreLevel'),
    storeRegionList?: [ string ](name='store_region_list', description='存储 Region 列表', example='cn-hangzhou', items='{"type":"string"}', x-go-name='StoreRegionList'),
    updatedAt?: string(name='updated_at', description='Domain 更新时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='UpdatedAt'),
  }

  /**
   * 
   */
  model GetUserAccessTokenRequest {
    role?: string(name='role', default='user', description='角色', enum='superadmin, admin, user', example='user', x-go-name='Role'),
    userId: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
  }

  /**
   * 
   */
  model ListAppsResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items: [ GetAppResponse ](name='items', description='App 列表', example='', items='{"$ref":"#/definitions/GetAppResponse"}', x-go-name='Items'),
    nextMarker?: string(name='next_marker', description='App 分批查询游标', example='ncsajnsalsa=', x-go-name='NextMarker'),
  }

  /**
   * list domain response
   */
  model ListDomainsResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseDomainResponse ](name='items', description='domain 列表', example='', items='{"$ref":"#/definitions/BaseDomainResponse"}', x-go-name='DomainList'),
    nextMarker?: string(name='next_marker', description='下次分页查询游标', example='NWQ1YmI4MjA0ZGU1ZWNjYzAzODM0ZDVkODlkMWJiMzcyNzM1NTU4OA', x-go-name='NextMarker'),
  }

  /**
   * 
   */
  model ListStoresResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items: [ Store ](name='items', description='Store 列表', example='', items='{"$ref":"#/definitions/Store"}', x-go-name='Items'),
  }

  /**
   * 
   */
  model Store {
    accelerateEndpoint?: string(name='accelerate_endpoint', description='全球加速域名', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='AccelerateEndpoint'),
    basePath?: string(name='base_path', description='存储公共前缀', example='ccp-data', x-go-name='BasePath'),
    bucket: string(name='bucket', description='bucket名称', example='data-sz-bucket', x-go-name='Bucket'),
    customizedEndpoint?: string(name='customized_endpoint', description='用户自定义绑定存储地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='CustomizedEndpoint'),
    endpoint: string(name='endpoint', description='存储访问地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='Endpoint'),
    internalEndpoint?: string(name='internal_endpoint', description='内网存储地址', example='https://oss-cn-shenzhen.aliyuncs.com/', x-go-name='InternalEndpoint'),
    ownership: string(name='ownership', description='存储归属，system表示系统提供，custom表示使用自己的存储', enum='system, custom', example='system', x-go-name='Ownership'),
    policy: string(name='policy', description='Policy授权,system类型store会将bucket权限授予当前云账号', x-go-name='Policy'),
    roleArn?: string(name='role_arn', description='访问Bucket的角色ARN', example='acs:oss:*:*:*', x-go-name='RoleArn'),
    storeId: string(name='store_id', description='store ID', example='cdjaksbwuiqbxuiassd', x-go-name='StoreID'),
    type: string(name='type', description='存储类型，当前只支持oss', enum='oss', example='oss', x-go-name='Type'),
  }

  /**
   * create domain response
   */
  model UpdateDomainResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    authAlipayAppId?: string(name='auth_alipay_app_id', description='支付宝 App Id', example='abc', x-go-name='AuthAlipayAppId'),
    authAlipayEnable?: boolean(name='auth_alipay_enable', description='是否开启了支付宝认证', example=true, x-go-name='AuthAlipayEnable'),
    authAlipayPrivateKey?: string(name='auth_alipay_private_key', description='支付宝 App Secret', example='abc', x-go-name='AuthAlipayPrivateKey'),
    authConfig?: object(name='auth_config', additionalProperties='{"$ref":"#/definitions/AuthConfig"}', description='登录相关信息', example='{}', x-go-name='AuthConfig'),
    authDingdingAppId?: string(name='auth_dingding_app_id', description='钉钉 App Id', example='abc', x-go-name='AuthDingdingAppId'),
    authDingdingAppSecret?: string(name='auth_dingding_app_secret', description='钉钉 App Secret', example='abc', x-go-name='AuthDingdingAppSecret'),
    authDingdingEnable?: boolean(name='auth_dingding_enable', description='是否开启了钉钉认证', example=true, x-go-name='AuthDingdingEnable'),
    authEndpointEnable?: boolean(name='auth_endpoint_enable', x-go-name='AuthEndpointEnable'),
    authRamAppId?: string(name='auth_ram_app_id', description='RAM App Id', example='abc', x-go-name='AuthRamAppId'),
    authRamAppSecret?: string(name='auth_ram_app_secret', description='RAM App Secret', example='abc', x-go-name='AuthRamAppSecret'),
    authRamEnable?: boolean(name='auth_ram_enable', description='是否开启了 RAM 认证', example=true, x-go-name='AuthRamEnable'),
    createdAt?: string(name='created_at', description='Domain 创建时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='CreatedAt'),
    dataHashName?: string(name='data_hash_name', description='数据 Hash 算法', example='sha1', x-go-name='DataHashName'),
    description?: string(name='description', description='Domain 描述', example='my test domain', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='sz111', x-go-name='DomainID'),
    domainName?: string(name='domain_name', description='Domain 描述', example='my test domain', x-go-name='DomainName'),
    eventFilenameMatches?: string(name='event_filename_matches', description='事件通知 MNS 匹配文件名', example='abc', x-go-name='EventFileNameMatches'),
    eventMnsEndpoint?: string(name='event_mns_endpoint', description='事件通知 MNS Endpoint', example='http://abc.mns.cn-shenzhen.aliyuncs.com', x-go-name='EventMnsEndpoint'),
    eventMnsTopic?: string(name='event_mns_topic', description='事件通知 MNS Topic', example='abc', x-go-name='EventMnsTopic'),
    eventNames?: [ string ](name='event_names', description='事件名列表', example='abc', items='{"type":"string"}', x-go-name='EventNames'),
    eventRoleArn?: string(name='event_role_arn', description='事件通知 Role Arn', x-go-name='EventRoleArn'),
    initDriveEnable?: boolean(name='init_drive_enable', description='是否开启了自动初始化 Drive', example=true, x-go-name='InitDriveEnable'),
    initDriveSize?: long(name='init_drive_size', description='自动初始化 Drive 大小', example=1024, format='int64', x-go-name='InitDriveSize'),
    initDriveStoreId?: string(name='init_drive_store_id', description='自动初始化 Drive 所用 Store ID', example='1', x-go-name='InitDriveStoreID'),
    pathType?: string(name='path_type', description='Domain 类型', example='CCPPath', x-go-name='PathType'),
    publishedAppAccessStrategy?: [ AppAccessStrategy ](name='published_app_access_strategy', $ref='#/definitions/AppAccessStrategy'),
    sharable?: boolean(name='sharable', description='是否开启了分享', example=true, x-go-name='Sharable'),
    storeLevel?: string(name='store_level', description='存储级别', example='Standard', x-go-name='StoreLevel'),
    storeRegionList?: [ string ](name='store_region_list', description='存储 Region 列表', example='cn-hangzhou', items='{"type":"string"}', x-go-name='StoreRegionList'),
    updatedAt?: string(name='updated_at', description='Domain 更新时间', example='"2019-08-31T12:58:31.137Z"', x-go-name='UpdatedAt'),
  }

  /**
   * complete file request
   */
  model BaseCompleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartList'),
    uploadId?: string(name='upload_id', description='upload_id', x-go-name='UploadID'),
  }

  /**
   * create file request
   */
  model BaseCreateFileRequest {
    contentMd5: string(name='content_md5', description='ContentMd5', example='E10ADC3949BA59ABBE56E057F20F883E', x-go-name='ContentMd5'),
    contentType: string(name='content_type', description='ContentType', example='application/json', x-go-name='ContentType'),
    name: string(name='name', description='Name', example='ccp.jpg', maximum=1024, minimum=1, pattern='[a-z0-9.-_]{1,1024}', x-go-name='Name'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    size: long(name='size', description='Size', format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    type: string(name='type', description='Type', enum='file, folder', example='file', x-go-name='Type'),
  }

  /**
   * 获取文件上传URL
   */
  model BaseGetUploadUrlRequest {
    contentMd5?: string(name='content_md5', description='content_md5', example='E10ADC3949BA59ABBE56E057F20F883E', maxLength=32, x-go-name='ContentMd5'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='upload_part_list', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    uploadId: string(name='upload_id', description='upload_id', example='3920F2BE4D9446D6967E2ED505A97EFD', x-go-name='UploadID'),
  }

  /**
   * list file request
   */
  model BaseListFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process', example='image/resize,w_200', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_url_process', example='image/resize,w_200', x-go-name='ImageUrlProcess'),
    limit?: long(name='limit', default=50, description='limit', example=10, format='int64', maximum=100, minimum=0, pattern='[0-9]{1, 3}', x-go-name='Limit'),
    marker?: string(name='marker', description='marker', example='NWQ1Yjk4YmI1ZDRlYmU1Y2E0YWE0NmJhYWJmODBhNDQ2NzhlMTRhMg', x-go-name='Marker'),
  }

  /**
   * 文件移动请求
   */
  model BaseMoveFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    newName?: string(name='new_name', description='new_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='NewName'),
    overwrite?: boolean(name='overwrite', default=false, description='overwrite
type: boolean', example=false, x-go-name='Overwrite'),
  }

  /**
   * 
   */
  model BatchSubRequest {
    body?: object(name='body', additionalProperties='{"type":"object"}', description='body 子请求的请求参数 json 字符串，可参考对于子请求文档, 当指定了body 必须传headers ： "Content-Type" 对应的类型，目前子请求入参是"application/json"', example='{"drive_id":"101","file_id":"xxxxxx"}', x-go-name='Body'),
    headers?: object(name='headers', additionalProperties='{"type":"string"}', description='headers 请求头，表示body传入数据的类型', example='{"Content-Type":"application/json"}', x-go-name='Headers'),
    id: string(name='id', description='id 用于request 和 response关联， 不允许重复', example='"1"', x-go-name='ID'),
    method: string(name='method', description='method', enum='POST, GET, PUT, DELETE, HEAD', example='"POST"', x-go-name='Method'),
    url: string(name='url', description='url 子请求的api path路径， 可参考对于子请求文档', example='/file/get', x-go-name='URL'),
  }

  /**
   * 批处理
   */
  model CCPBatchRequest {
    requests: [ BatchSubRequest ](name='requests', description='Requests 请求合集', items='{"$ref":"#/definitions/BatchSubRequest"}', x-go-name='Requests'),
    resource: string(name='resource', description='支持的资源类型', example='"file"', x-go-name='Resource'),
  }

  /**
   * 合并文件上传任务
   */
  model CCPCompleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartList'),
    uploadId?: string(name='upload_id', description='upload_id', x-go-name='UploadID'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1,50}', x-go-name='FileID'),
  }

  /**
   * 文件拷贝
   */
  model CCPCopyFileRequest {
    autoRename?: boolean(name='auto_rename', default=false, description='auto_rename
type: boolean', example=false, x-go-name='AutoRename'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    newName: string(name='new_name', description='new_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='NewName'),
    toDriveId: string(name='to_drive_id', description='to_drive_id', example='1', pattern='[0-9]+', x-go-name='ToDriveID'),
    toParentFileId: string(name='to_parent_file_id', description='to_parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='ToParentFileId'),
  }

  /**
   * 创建文件
   */
  model CCPCreateFileRequest {
    contentMd5: string(name='content_md5', description='ContentMd5', example='E10ADC3949BA59ABBE56E057F20F883E', x-go-name='ContentMd5'),
    contentType: string(name='content_type', description='ContentType', example='application/json', x-go-name='ContentType'),
    name: string(name='name', description='Name', example='ccp.jpg', maximum=1024, minimum=1, pattern='[a-z0-9.-_]{1,1024}', x-go-name='Name'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    size: long(name='size', description='Size', format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    type: string(name='type', description='Type', enum='file, folder', example='file', x-go-name='Type'),
    autoRename?: boolean(name='auto_rename', default=false, description='auto_rename', enum='false, true', x-go-name='AutoRename'),
    contentHash?: string(name='content_hash', description='content_hash', example='7C4A8D09CA3762AF61E59520943DC26494F8941B', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='content_hash_name', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    description?: string(name='description', description='description', example='file description', maxLength=0, minLength=1024, x-go-name='Description'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    hidden?: boolean(name='hidden', description='hidden', enum='true, false', example=false, x-go-name='Hidden'),
    labels?: [ string ](name='labels', description='labels', example='label1, label2', items='{"type":"string"}', x-go-name='Labels'),
    meta?: string(name='meta', description='meta', example='meta test', x-go-name='Meta'),
    parentFileId: string(name='parent_file_id', description='parent_file_id', example='root', maxLength=50, minLength=40, pattern='[a-z0-9]{1,50}', x-go-name='ParentFileID'),
    preHash?: string(name='pre_hash', description='pre_hash', example='E10ADC3949BA59ABBE56E057F20F883E', x-go-name='PreHash'),
  }

  /**
   * 删除文件请求
   */
  model CCPDeleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', example='5d792010669a1dd91087479e8071c9ae212065cc', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    permanently?: boolean(name='permanently', default=false, description='permanently
type: false', example=false, x-go-name='Permanently'),
  }

  /**
   * 批量删除文件请求
   */
  model CCPDeleteFilesRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileIdList: [ string ](name='file_id_list', description='file_id_list', example='', items='{"type":"string"}', maxItems=100, x-go-name='FileIDList'),
  }

  /**
   * 获取异步人去信息
   */
  model CCPGetAsyncTaskRequest {
    asyncTaskId?: string(name='async_task_id', description='async_task_id
type:string', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
  }

  /**
   * 获取文件下载地址的请求body
   */
  model CCPGetDownloadUrlRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    expireSec?: long(name='expire_sec', default=900, description='expire_sec', example=15, format='int64', maximum=14400, minimum=0, x-go-name='ExpireSec'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    fileName: string(name='file_name', description='file_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='FileName'),
  }

  /**
   * 获取文件元数据
   */
  model CCPGetFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process
type:string', example='image/resize,w_200', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_thumbnail_process
type:string', example='image/crop,w_100,h_100/rounded-corners,r_10/format,png', x-go-name='ImageUrlProcess'),
  }

  /**
   * 获取文件上传URL
   */
  model CCPGetUploadUrlRequest {
    contentMd5?: string(name='content_md5', description='content_md5', example='E10ADC3949BA59ABBE56E057F20F883E', maxLength=32, x-go-name='ContentMd5'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='upload_part_list', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    uploadId: string(name='upload_id', description='upload_id', example='3920F2BE4D9446D6967E2ED505A97EFD', x-go-name='UploadID'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9]{1,50}', x-go-name='FileID'),
  }

  /**
   * 列举uploadID对应的已上传分片
   */
  model CCPListUploadedPartRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    limit: long(name='limit', default=1000, description='limit', example=1, format='int64', maximum=1000, minimum=1, pattern='[0-9]+', x-go-name='Limit'),
    partNumberMarker?: long(name='part_number_marker', description='part_number_marker', example=1, format='int64', minimum=1, pattern='[0-9]+', x-go-name='PartNumberMarker'),
    uploadId?: string(name='upload_id', description='upload_id', example='00668396C0814D818D90F0A92B04B355', x-go-name='UploadID'),
  }

  /**
   * 文件移动请求
   */
  model CCPMoveFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    newName?: string(name='new_name', description='new_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='NewName'),
    overwrite?: boolean(name='overwrite', default=false, description='overwrite
type: boolean', example=false, x-go-name='Overwrite'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    toParentFileId: string(name='to_parent_file_id', description='to_parent_file_id', example='root', maxLength=50, minLength=40, x-go-name='ToParentFileId'),
  }

  /**
   * 搜索文件元数据
   */
  model CCPSearchFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process', example='image/resize,m_fill,h_128,w_128,limit_0', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_url_process', example='image/resize,m_fill,h_128,w_128,limit_0', x-go-name='ImageUrlProcess'),
    limit?: integer(name='limit', default=50, description='limit', example=10, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='Marker', example='""', x-go-name='Marker'),
    orderBy?: string(name='order_by', default='updated_at', description='order_by', enum='name, created_at, updated_at, size', example='size', x-go-name='OrderBy'),
    query?: string(name='query', description='query', example='not name=123', maxLength=4096, x-go-name='Query'),
  }

  /**
   * 更新文件元数据
   */
  model CCPUpdateFileMetaRequest {
    customIndexKey?: string(name='custom_index_key', x-go-name='CustomIndexKey'),
    description?: string(name='description', description='description
type: string', example='description', maxLength=1024, x-go-name='Description'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', example='5d5b846942cf94fa72324c14a4bda34e81da635d', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    hidden?: boolean(name='hidden', default=false, description='hidden
type: boolean', example=false, x-go-name='Hidden'),
    labels?: [ string ](name='labels', description='labels', example='label1, label2', items='{"type":"string"}', x-go-name='Labels'),
    meta?: string(name='meta', description='meta', example='meta test', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    starred?: boolean(name='starred', default=false, description='starred
type: boolean', example=false, x-go-name='Starred'),
  }

  /**
   * complete file request
   */
  model CompleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9]{1,50}', x-go-name='FileID'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartList'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
    uploadId: string(name='upload_id', description='upload_id', x-go-name='UploadID'),
  }

  /**
   * 文件移动请求
   */
  model CopyFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    newName: string(name='new_name', description='new_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='NewName'),
    overwrite?: boolean(name='overwrite', default=false, description='overwrite
type: boolean', x-go-name='Overwrite'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
    toParentFileId: string(name='to_parent_file_id', description='to_parent_file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='ToParentFileId'),
    toParentFilePath?: string(name='to_parent_file_path', x-go-name='ToParentFilePath'),
  }

  /**
   * create drive request
   */
  model CreateDriveRequest {
    default?: boolean(name='default', default=false, description='是否默认drive, 只允许设置一个默认drive', example=true, x-go-name='Default'),
    description?: string(name='description', description='描述信息', example='ccp team drive', maximum=1024, x-go-name='Description'),
    driveName: string(name='drive_name', description='Drive 名称', example='ccpdrive', maximum=1024, x-go-name='DriveName'),
    driveType?: string(name='drive_type', default='normal', description='Drive类型', enum='normal, large', example='normal', x-go-name='DriveType'),
    owner: string(name='owner', description='所属者', example='ccp-001', x-go-name='Owner'),
    relativePath?: string(name='relative_path', description='domain的PathType为OSSPath时必选。 Drive存储基于store的相对路径', example='/d/e/f/', x-go-name='RelativePath'),
    status?: string(name='status', default='enabled', description='状态', enum='disabled, enabled', example='enabled', x-go-name='Status'),
    storeId?: string(name='store_id', description='StoreID , domain的PathType为OSSPath时必选', example='store_id', x-go-name='StoreID'),
    totalSize?: long(name='total_size', default=-1, description='总大小,单位Byte [如果设置 -1 代表不限制]', example=1024, format='int64', x-go-name='TotalSize'),
  }

  /**
   * create file request
   */
  model CreateFileRequest {
    contentHash?: string(name='content_hash', description='ContentHash', x-go-name='ContentHash'),
    contentHashName?: string(name='content_hash_name', description='ContentHashName', enum='sha1', example='sha1', x-go-name='ContentHashName'),
    contentMd5?: string(name='content_md5', description='ContentMd5', maxLength=32, x-go-name='ContentMd5'),
    contentType: string(name='content_type', description='ContentType', x-go-name='ContentType'),
    description?: string(name='description', description='Description', maxLength=0, minLength=1024, x-go-name='Description'),
    driveId: string(name='drive_id', description='DriveID', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    hidden?: boolean(name='hidden', description='Hidden', enum='true, false', x-go-name='Hidden'),
    meta?: string(name='meta', description='Meta', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    parentFileId: string(name='parent_file_id', description='parent_file_id', maxLength=50, minLength=40, pattern='[a-z0-9]{1,50}', x-go-name='ParentFileID'),
    parentFilePath?: string(name='parent_file_path', description='ParentFilePath', x-go-name='ParentFilePath'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    preHash?: string(name='pre_hash', description='pre_hash', x-go-name='PreHash'),
    shareId?: string(name='share_id', description='ShareID', x-go-name='ShareID'),
    size: long(name='size', description='Size', format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    tags?: object(name='tags', additionalProperties='{"type":"object"}', description='tags', x-go-name='Tags'),
    type: string(name='type', description='Type', enum='file, folder', x-go-name='Type'),
  }

  /**
   * create share request
   */
  model CreateShareRequest {
    description?: string(name='description', description='description', example='share description', x-go-name='Description'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z', x-go-name='Expiration'),
    owner: string(name='owner', description='creator', example='abc', x-go-name='Owner'),
    permissions?: [ string ](name='permissions', description='permissions', example='', items='{"type":"string"}', x-go-name='Permissions'),
    shareFilePath: string(name='share_file_path', description='share_file_path', x-go-name='ShareFilePath'),
    shareName?: string(name='share_name', description='share_name', example='share', x-go-name='ShareName'),
    status?: string(name='status', description='status', enum='enabled, disabled', example='enabled', x-go-name='Status'),
  }

  /**
   * Delete drive request
   */
  model DeleteDriveRequest {
    driveId: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
  }

  /**
   * 删除文件请求
   */
  model DeleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    permanently?: boolean(name='permanently', x-go-name='Permanently'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
  }

  /**
   * delete share request
   */
  model DeleteShareRequest {
    shareId: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
  }

  /**
   * 下载文件请求body
   */
  model DownloadRequest {
    DriveID: string(name='DriveID', description='drive_id', example='1', pattern='[0-9]+'),
    FileID: string(name='FileID', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}'),
    ImageProcess?: string(name='ImageProcess', description='image_process', example='image/resize,w_200'),
    ShareID?: string(name='ShareID'),
  }

  /**
   * 获取异步人去信息
   */
  model GetAsyncTaskRequest {
    asyncTaskId?: string(name='async_task_id', description='async_task_id
type:string', example='000e89fb-cf8f-11e9-8ab4-b6e980803a3b', x-go-name='AsyncTaskID'),
  }

  /**
   * Get default drive request
   */
  model GetDefaultDriveRequest {
    userId?: string(name='user_id', description='用户ID', example='abc', x-go-name='UserID'),
  }

  /**
   * 获取文件下载地址的请求body
   */
  model GetDownloadUrlRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    expireSec?: long(name='expire_sec', default=900, description='expire_sec', example=15, format='int64', maximum=14400, minimum=0, x-go-name='ExpireSec'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    fileName: string(name='file_name', description='file_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='FileName'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
  }

  /**
   * Get drive request
   */
  model GetDriveRequest {
    driveId: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
  }

  /**
   * 获取文件元数据
   */
  model GetFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process
type:string', example='image/resize,w_200', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_thumbnail_process
type:string', example='image/resize,w_200', x-go-name='ImageUrlProcess'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
  }

  /**
   * get share request
   */
  model GetShareRequest {
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
  }

  /**
   * 获取文件上传URL
   */
  model GetUploadUrlRequest {
    contentMd5?: string(name='content_md5', description='content_md5', maxLength=32, x-go-name='ContentMd5'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9]{1,50}', x-go-name='FileID'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='upload_part_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
    uploadId: string(name='upload_id', description='upload_id', x-go-name='UploadID'),
  }

  /**
   * List drive request
   */
  model ListDriveRequest {
    limit?: integer(name='limit', default=100, description='每页大小限制', example=10, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='翻页标记, 接口返回的标记值', example='marker', x-go-name='Marker'),
    owner?: string(name='owner', description='所属者', example='abc', x-go-name='Owner'),
  }

  /**
   * list file request
   */
  model ListFileRequest {
    all?: boolean(name='all', description='all', enum='false,  true', x-go-name='All'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_url_process', x-go-name='ImageUrlProcess'),
    limit?: long(name='limit', default=50, description='limit', format='int64', maximum=100, minimum=0, pattern='[0-9]{1, 3}', x-go-name='Limit'),
    marker?: string(name='marker', description='marker', x-go-name='Marker'),
    parentFileId: string(name='parent_file_id', description='ParentFileID', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='ParentFileID'),
    parentFilePath?: string(name='parent_file_path', x-go-name='ParentFilePath'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
    status?: string(name='status', description='status', enum='uploading,  available', x-go-name='Status'),
  }

  /**
   * List my drive request
   */
  model ListMyDriveRequest {
    limit?: integer(name='limit', default=100, description='每页大小限制', example=10, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='翻页标记, 接口返回的标记值', example='marker', x-go-name='Marker'),
  }

  /**
   * list share request
   */
  model ListShareRequest {
    creator: string(name='creator', description='creator', example='abc', x-go-name='Creator'),
    limit: integer(name='limit', description='limit', example=50, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='marker', x-go-name='Marker'),
    owner: string(name='owner', description='creator', example='edf', x-go-name='Owner'),
  }

  /**
   * list store file
   */
  model ListStoreFileRequest {
    limit?: long(name='limit', default=100, description='limit', example=50, format='int64', maximum=1000, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='marker', x-go-name='Marker'),
    parentFilePath?: string(name='parent_file_path', description='parent_file_path', x-go-name='ParentFilePath'),
    storeId?: string(name='store_id', description='store_id', example='xxx', x-go-name='StoreID'),
    type?: string(name='type', description='type', enum='file, folder', example='file', x-go-name='Type'),
  }

  /**
   * list storage file
   */
  model ListStoreRequest {
    domainId?: string(name='domain_id', description='domain_id', example='10', x-go-name='DomainID'),
  }

  /**
   * complete file request
   */
  model OSSCompleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartList'),
    uploadId?: string(name='upload_id', description='upload_id', x-go-name='UploadID'),
    filePath?: string(name='file_path', x-go-name='FilePath'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
  }

  /**
   * copy file request
   */
  model OSSCopyFileRequest {
    driveId?: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    newName: string(name='new_name', description='new_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='NewName'),
    overwrite?: boolean(name='overwrite', default=false, description='overwrite
type: boolean', x-go-name='Overwrite'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', pattern='[0-9a-zA-z-]+', x-go-name='ShareID'),
    toDriveId: string(name='to_drive_id', description='to_drive_id', example='1', pattern='[0-9]+', x-go-name='ToDriveID'),
    toParentFilePath: string(name='to_parent_file_path', description='to_parent_file_path', example='/a/b/c/', x-go-name='ToParentFilePath'),
    toShareId?: string(name='to_share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ToShareID'),
  }

  /**
   * create file request
   */
  model OSSCreateFileRequest {
    contentMd5: string(name='content_md5', description='ContentMd5', example='E10ADC3949BA59ABBE56E057F20F883E', x-go-name='ContentMd5'),
    contentType: string(name='content_type', description='ContentType', example='application/json', x-go-name='ContentType'),
    name: string(name='name', description='Name', example='ccp.jpg', maximum=1024, minimum=1, pattern='[a-z0-9.-_]{1,1024}', x-go-name='Name'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='part_info_list', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    size: long(name='size', description='Size', format='int64', maximum=53687091200, minimum=0, x-go-name='Size'),
    type: string(name='type', description='Type', enum='file, folder', example='file', x-go-name='Type'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    parentFilePath: string(name='parent_file_path', description='parent_file_path', x-go-name='ParentFilePath'),
    shareId?: string(name='share_id', description='share_id', maxLength=40, minLength=50, x-go-name='ShareID'),
  }

  /**
   * 删除文件请求
   */
  model OSSDeleteFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath: string(name='file_path', description='file_path', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FilePath'),
    permanently?: boolean(name='permanently', description='permanently
type: false', example=false, x-go-name='Permanently'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', pattern='[0-9a-zA-z-]+', x-go-name='ShareID'),
  }

  /**
   * 获取文件下载地址的请求body
   */
  model OSSGetDownloadUrlRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    expireSec?: long(name='expire_sec', default=900, description='expire_sec', example=15, format='int64', maximum=14400, minimum=0, x-go-name='ExpireSec'),
    fileName: string(name='file_name', description='file_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='FileName'),
    filePath: string(name='file_path', description='file_path', example='/a/b/c.jpg', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FilePath'),
    shareId: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', pattern='[0-9a-z-]+', x-go-name='ShareID'),
  }

  /**
   * 获取文件元数据
   */
  model OSSGetFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath: string(name='file_path', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FilePath'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process
type:string', example='image/resize,w_200', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_thumbnail_process
type:string', example='image/resize,w_200', x-go-name='ImageUrlProcess'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', pattern='[0-9a-zA-z-]+', x-go-name='ShareID'),
  }

  /**
   * 获取文件上传URL
   */
  model OSSGetUploadUrlRequest {
    contentMd5?: string(name='content_md5', description='content_md5', example='E10ADC3949BA59ABBE56E057F20F883E', maxLength=32, x-go-name='ContentMd5'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    partInfoList?: [ UploadPartInfo ](name='part_info_list', description='upload_part_list', example='', items='{"$ref":"#/definitions/UploadPartInfo"}', x-go-name='PartInfoList'),
    uploadId: string(name='upload_id', description='upload_id', example='3920F2BE4D9446D6967E2ED505A97EFD', x-go-name='UploadID'),
    filePath: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    shareId: string(name='share_id', description='share_id', example='1', pattern='[0-9]+', x-go-name='ShareID'),
  }

  /**
   * list file request
   */
  model OSSListFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    imageThumbnailProcess?: string(name='image_thumbnail_process', description='image_thumbnail_process', example='image/resize,w_200', x-go-name='ImageThumbnailProcess'),
    imageUrlProcess?: string(name='image_url_process', description='image_url_process', example='image/resize,w_200', x-go-name='ImageUrlProcess'),
    limit?: long(name='limit', default=50, description='limit', example=10, format='int64', maximum=100, minimum=0, pattern='[0-9]{1, 3}', x-go-name='Limit'),
    marker?: string(name='marker', description='marker', example='NWQ1Yjk4YmI1ZDRlYmU1Y2E0YWE0NmJhYWJmODBhNDQ2NzhlMTRhMg', x-go-name='Marker'),
    parentFilePath: string(name='parent_file_path', description='ParentFilePath', example='/a/b/c.jpg', x-go-name='ParentFilePath'),
    shareId: string(name='share_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='ShareID'),
  }

  /**
   * 列举uploadID对应的已上传分片
   */
  model OSSListUploadedPartRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    limit: long(name='limit', default=1000, description='limit', example=1, format='int64', maximum=1000, minimum=1, pattern='[0-9]+', x-go-name='Limit'),
    partNumberMarker?: long(name='part_number_marker', description='part_number_marker', example=1, format='int64', minimum=1, pattern='[0-9]+', x-go-name='PartNumberMarker'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', pattern='[0-9a-zA-z-]+', x-go-name='ShareID'),
    uploadId?: string(name='upload_id', description='upload_id', example='00668396C0814D818D90F0A92B04B355', x-go-name='UploadID'),
  }

  /**
   * 文件移动请求
   */
  model OSSMoveFileRequest {
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    filePath?: string(name='file_path', description='file_path', example='/a/b/c.jpg', x-go-name='FilePath'),
    newName: string(name='new_name', description='new_name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='NewName'),
    overwrite?: boolean(name='overwrite', default=false, description='overwrite
type: boolean', x-go-name='Overwrite'),
    shareId?: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', pattern='[0-9a-zA-z-]+', x-go-name='ShareID'),
    toParentFilePath?: string(name='to_parent_file_path', description='file_path', example='/a/b/c/', x-go-name='ToParentFilePath'),
  }

  /**
   * UCGetObjectInfoByObjectKeyRequest
   */
  model UCGetObjectInfoByObjectKeyRequest {
    objectKey?: string(name='object_key', x-go-name='ObjectKey'),
  }

  /**
   * UCGetObjectInfoBySha1Request
   */
  model UCGetObjectInfoBySha1Request {
    sha1?: string(name='sha1', x-go-name='Sha1'),
  }

  /**
   * Update drive request
   */
  model UpdateDriveRequest {
    description?: string(name='description', description='描述信息', example='ccp team drive', maximum=1024, x-go-name='Description'),
    driveId: string(name='drive_id', description='Drive ID', example='123', x-go-name='DriveID'),
    driveName: string(name='drive_name', description='Drive 名称', example='ccpdrive', maximum=1024, x-go-name='DriveName'),
    status?: string(name='status', default='enabled', description='状态', enum='disabled, enabled', example='enabled', x-go-name='Status'),
    totalSize?: long(name='total_size', default=0, description='总大小,单位Byte [如果设置 0 代表不限制]', example=1024, format='int64', x-go-name='TotalSize'),
  }

  /**
   * 更新文件元数据
   */
  model UpdateFileMetaRequest {
    description?: string(name='description', description='description
type: string', maxLength=1000, x-go-name='Description'),
    driveId: string(name='drive_id', description='drive_id', example='1', pattern='[0-9]+', x-go-name='DriveID'),
    fileId: string(name='file_id', description='file_id', maxLength=50, minLength=40, pattern='[a-z0-9.-_]{1,50}', x-go-name='FileID'),
    hidden?: boolean(name='hidden', default=false, description='hidden
type: boolean', x-go-name='Hidden'),
    meta?: string(name='meta', description='meta', x-go-name='Meta'),
    name: string(name='name', description='name', example='ccp.jpg', pattern='[a-zA-Z0-9.-]{1,1024}', x-go-name='Name'),
    shareId?: string(name='share_id', x-go-name='ShareID'),
    starred?: boolean(name='starred', default=false, description='starred
type: boolean', x-go-name='Starred'),
    tags?: object(name='tags', additionalProperties='{"type":"object"}', description='tags', x-go-name='Tags'),
  }

  /**
   * update share request
   */
  model UpdateShareRequest {
    description?: string(name='description', description='description', example='share description', maxLength=1024, x-go-name='Description'),
    expiration?: string(name='expiration', description='expiration', example='2006-01-02T15:04:05.999Z', x-go-name='ExpireTime'),
    permissions?: [ string ](name='permissions', description='permissions', example='', items='{"type":"string"}', x-go-name='Permissions'),
    shareId: string(name='share_id', description='share_id', example='3d336314-63c8-4d96-bce0-17aefb6833b6', x-go-name='ShareID'),
    shareName?: string(name='share_name', description='share_name', example='new_share_name', x-go-name='ShareName'),
    status?: string(name='status', description='status', enum='enabled, disabled', example='enabled', x-go-name='Status'),
  }

  /**
   * Base user response
   */
  model BaseUserResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    avatar?: string(name='avatar', description='头像', example='http://a.b.c/ccp.jpg', x-go-name='Avatar'),
    createdAt?: long(name='created_at', description='用户创建时间', example=1567407718386, format='int64', x-go-name='CreatedAt'),
    defaultDriveId?: string(name='default_drive_id', description='默认 Drive ID', example='123', x-go-name='DefaultDriveID'),
    description?: string(name='description', description='用户备注信息', example='ccp team user', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    email?: string(name='email', description='邮箱', example='123@ccp.com', x-go-name='Email'),
    nickName?: string(name='nick_name', description='昵称', example='abc', x-go-name='NickName'),
    phone?: string(name='phone', description='电话', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', description='角色', example='user', x-go-name='Role'),
    status?: string(name='status', description='用户状态', example='enabled', x-go-name='Status'),
    updatedAt?: long(name='updated_at', description='用户修改时间', example=1567407718386, format='int64', x-go-name='UpdatedAt'),
    userId?: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
    userName?: string(name='user_name', description='用户名称', example='name', x-go-name='UserName'),
  }

  /**
   * Create user request
   */
  model CreateUserRequest {
    avatar?: string(name='avatar', description='头像', example='http://a.b.c/ccp.jpg', x-go-name='Avatar'),
    description?: string(name='description', description='描述信息', example='ccp team user', maximum=1024, x-go-name='Description'),
    email?: string(name='email', description='邮箱', example='123@ccp.com', x-go-name='Email'),
    nickName?: string(name='nick_name', description='昵称', example='ccpuser', x-go-name='NickName'),
    phone?: string(name='phone', description='电话号码', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', default='user', description='角色', enum='superadmin,  admin,  user', example='user', x-go-name='Role'),
    status?: string(name='status', default='enabled', description='状态', enum='enabled,  disabled', example='enabled', x-go-name='Status'),
    userId: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
    userName?: string(name='user_name', description='用户名称', example='abc', x-go-name='UserName'),
  }

  /**
   * Create user response
   */
  model CreateUserResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    avatar?: string(name='avatar', description='头像', example='http://a.b.c/ccp.jpg', x-go-name='Avatar'),
    createdAt?: long(name='created_at', description='用户创建时间', example=1567407718386, format='int64', x-go-name='CreatedAt'),
    defaultDriveId?: string(name='default_drive_id', description='默认 Drive ID', example='123', x-go-name='DefaultDriveID'),
    description?: string(name='description', description='用户备注信息', example='ccp team user', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    email?: string(name='email', description='邮箱', example='123@ccp.com', x-go-name='Email'),
    nickName?: string(name='nick_name', description='昵称', example='abc', x-go-name='NickName'),
    phone?: string(name='phone', description='电话', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', description='角色', example='user', x-go-name='Role'),
    status?: string(name='status', description='用户状态', example='enabled', x-go-name='Status'),
    updatedAt?: long(name='updated_at', description='用户修改时间', example=1567407718386, format='int64', x-go-name='UpdatedAt'),
    userId?: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
    userName?: string(name='user_name', description='用户名称', example='name', x-go-name='UserName'),
  }

  /**
   * Delete user request
   */
  model DeleteUserRequest {
    userId: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
  }

  /**
   * Delete user response
   */
  model DeleteUserResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
  }

  /**
   * Get user request
   */
  model GetUserRequest {
    userId?: string(name='user_id', description='用户 ID, 使用ak方式访问，该项必传, access_token访问如果不传，默认取自己的user信息
example', x-go-name='UserID'),
  }

  /**
   * Get user response
   */
  model GetUserResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    avatar?: string(name='avatar', description='头像', example='http://a.b.c/ccp.jpg', x-go-name='Avatar'),
    createdAt?: long(name='created_at', description='用户创建时间', example=1567407718386, format='int64', x-go-name='CreatedAt'),
    defaultDriveId?: string(name='default_drive_id', description='默认 Drive ID', example='123', x-go-name='DefaultDriveID'),
    description?: string(name='description', description='用户备注信息', example='ccp team user', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    email?: string(name='email', description='邮箱', example='123@ccp.com', x-go-name='Email'),
    nickName?: string(name='nick_name', description='昵称', example='abc', x-go-name='NickName'),
    phone?: string(name='phone', description='电话', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', description='角色', example='user', x-go-name='Role'),
    status?: string(name='status', description='用户状态', example='enabled', x-go-name='Status'),
    updatedAt?: long(name='updated_at', description='用户修改时间', example=1567407718386, format='int64', x-go-name='UpdatedAt'),
    userId?: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
    userName?: string(name='user_name', description='用户名称', example='name', x-go-name='UserName'),
  }

  /**
   * List user request
   */
  model ListUserRequest {
    limit?: integer(name='limit', default=100, description='每页大小限制', example=10, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='翻页标记', example='marker', x-go-name='Marker'),
  }

  /**
   * List user response
   */
  model ListUserResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    items?: [ BaseUserResponse ](name='items', items='{"$ref":"#/definitions/BaseUserResponse"}', x-go-name='Items'),
    nextMarker?: string(name='next_marker', description='翻页标记', example='nextmarker', x-go-name='NextMarker'),
  }

  /**
   * Search user request
   */
  model SearchUserRequest {
    email?: string(name='email', description='邮箱', example='abc@ccp.com', x-go-name='Email'),
    limit?: integer(name='limit', default=100, description='每页大小限制', example=10, format='int32', maximum=100, minimum=1, x-go-name='Limit'),
    marker?: string(name='marker', description='翻页标记', example='marker', x-go-name='Marker'),
    nickName?: string(name='nick_name', description='昵称', example='la', x-go-name='NickName'),
    phone?: string(name='phone', description='电话号码', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', default='user', description='角色', enum='superadmin,  admin,  user', example='user', x-go-name='Role'),
    status?: string(name='status', default='enabled', description='状态', enum='enabled,  disabled', example='enabled', x-go-name='Status'),
    userName?: string(name='user_name', description='用户名', example='abc', x-go-name='UserName'),
  }

  /**
   * Update user request
   */
  model UpdateUserRequest {
    avatar?: string(name='avatar', description='头像', example='http://a.b.c/ccp.jpg', x-go-name='Avatar'),
    description?: string(name='description', description='描述信息', example='ccp team user', maximum=1024, x-go-name='Description'),
    email?: string(name='email', description='邮箱', example='123@ccp.com', x-go-name='Email'),
    nickName?: string(name='nick_name', description='昵称', example='ccpuser', x-go-name='NickName'),
    phone?: string(name='phone', description='电话号码', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', default='user', description='角色', enum='superadmin,  admin,  user', example='user', x-go-name='Role'),
    status?: string(name='status', default='enabled', description='状态', enum='enabled,  disabled', example='enabled', x-go-name='Status'),
    userId: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
  }

  /**
   * Update user response
   */
  model UpdateUserResponse {
    requestId?: string(name='requestId', description='request Id', example='0BA8CF68-9769E-4469-A14B-8772004287E2', x-go-name='requestId'),
    avatar?: string(name='avatar', description='头像', example='http://a.b.c/ccp.jpg', x-go-name='Avatar'),
    createdAt?: long(name='created_at', description='用户创建时间', example=1567407718386, format='int64', x-go-name='CreatedAt'),
    defaultDriveId?: string(name='default_drive_id', description='默认 Drive ID', example='123', x-go-name='DefaultDriveID'),
    description?: string(name='description', description='用户备注信息', example='ccp team user', x-go-name='Description'),
    domainId?: string(name='domain_id', description='Domain ID', example='hz999', x-go-name='DomainID'),
    email?: string(name='email', description='邮箱', example='123@ccp.com', x-go-name='Email'),
    nickName?: string(name='nick_name', description='昵称', example='abc', x-go-name='NickName'),
    phone?: string(name='phone', description='电话', example='13700000000', x-go-name='Phone'),
    role?: string(name='role', description='角色', example='user', x-go-name='Role'),
    status?: string(name='status', description='用户状态', example='enabled', x-go-name='Status'),
    updatedAt?: long(name='updated_at', description='用户修改时间', example=1567407718386, format='int64', x-go-name='UpdatedAt'),
    userId?: string(name='user_id', description='用户 ID', example='ccpuserid', x-go-name='UserID'),
    userName?: string(name='user_name', description='用户名称', example='name', x-go-name='UserName'),
  }

  /**
   * 取消绑定关系，生成新用户，返回访问令牌
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api cancelLink(request: CancelLinkRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/cancel_link`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 确认绑定关系, 成功后返回访问令牌
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api confirmLink(request: ConfirmLinkRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/confirm_link`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 修改手机登录密码，密码必须包含数字和字母，长度8-32个字符
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api changePassword(request: DefaultChangePasswordRequest, runtime: RuntimeOptions): void  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/default/change_password`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 204)) {
      return;
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 设置手机登录密码，密码必须包含数字和字母，长度8-32个字符
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api setPassword(request: DefaultSetPasswordRequest, runtime: RuntimeOptions): void  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/default/set_password`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 204)) {
      return;
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 管理员通过账号信息直接获取用户的访问令牌
   * @tags account
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api getAccessTokenByLinkInfo(request: GetAccessTokenByLinkInfoRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/get_access_token_by_link_info`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 获取图片验证码，用于人机校验，默认不需要
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api getCaptcha(request: GetCaptchaRequest, runtime: RuntimeOptions): Captcha  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/get_captcha`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 获取用户认证方式详情
   * @tags account
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api getLinkInfo(request: GetByLinkInfoRequest, runtime: RuntimeOptions): LinkInfoResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/get_link_info`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 获取用户的所有绑定信息
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api getLinkInfoByUserId(request: GetLinkInfoByUserIDRequest, runtime: RuntimeOptions): LinkInfoListResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/get_link_info_by_user_id`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 绑定用户认证方式
   * @tags account
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error AlreadyExist {resource} has already exists. {extra_msg}
   * @error InternalError The request has been failed due to some unknown error.
   */
  api link(request: AccountLinkRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/link`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 查询手机号是否已被注册
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api checkExist(request: MobileCheckExistRequest, runtime: RuntimeOptions): MobileCheckExistResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/mobile/check_exist`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 通过手机号+短信或密码登录，返回刷新令牌和访问令牌
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api login(request: MobileLoginRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/mobile/login`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 通过手机号+短信验证码注册账号
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error AlreadyExist {resource} has already exists. {extra_msg}
   * @error InternalError The request has been failed due to some unknown error.
   */
  api register(request: MobileRegisterRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/mobile/register`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 发送短信验证码，用于登录、注册、修改密码、绑定等
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api mobileSendSmsCode(request: MobileSendSmsCodeRequest, runtime: RuntimeOptions): MobileSendSmsCodeResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/mobile/send_sms_code`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 用户通过刷新令牌（refresh_token）获取访问令牌（access_token）
   * @tags account
   * @error InvalidParameterMissing The input parameter {parameter_name} is missing.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api token(request: TokenRequest, runtime: RuntimeOptions): AccountAccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/account/token`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.auth.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 列举Store列表
   * @tags admin
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error Forbidden User not authorized to operate on the specified APIs.
   * @error InternalError The request has been failed due to some unknown error.
   */
  api adminListStores(request: AdminListStoresRequest, runtime: RuntimeOptions): ListStoresResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/domain/list_stores`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * This will get a user info
   * @tags admin
   * @error undefined undefined
   * @error undefined undefined
   * @error undefined undefined
   * @error undefined undefined
   * @error undefined undefined
   */
  api getAccessToken(request: GetUserAccessTokenRequest, runtime: RuntimeOptions): AccessTokenResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/get_access_token`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 可以指定 Drive 类型来创建 Drive
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api createDrive(request: CreateDriveRequest, runtime: RuntimeOptions): CreateDriveResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/create`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 201)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 删除指定drive_id对应的Drive
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api deleteDrive(request: DeleteDriveRequest, runtime: RuntimeOptions): void  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/delete`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 204)) {
      return;
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 获取指定drive_id对应的Drive详细信息。
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getDrive(request: GetDriveRequest, runtime: RuntimeOptions): GetDriveResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/get`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 一个用户可拥有多个drive，在创建drive时通过参数指定是否为这个用户的默认drive，
每个用户只能设置一个默认drive。
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getDefaultDrive(request: GetDefaultDriveRequest, runtime: RuntimeOptions): GetDriveResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/get_default_drive`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 管理员列举指定用户的Drive
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listDrives(request: ListDriveRequest, runtime: RuntimeOptions): ListDriveResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/list`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 列举当前用户（访问令牌）的Drive
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listMyDrives(request: ListMyDriveRequest, runtime: RuntimeOptions): ListDriveResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/list_my_drives`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 更新指定drive_id的Drive信息
   * @tags drive
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api updateDrive(request: UpdateDriveRequest, runtime: RuntimeOptions): UpdateDriveResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/drive/update`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 完成文件上传
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api completeFile(request: OSSCompleteFileRequest, runtime: RuntimeOptions): OSSCompleteFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/complete`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 指定源文件或文件夹路径，拷贝到指定的文件夹。
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api copyFile(request: OSSCopyFileRequest, runtime: RuntimeOptions): OSSCopyFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/copy`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 201)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 创建文件或者文件夹。
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error AlreadyExist {resource} has already exists. {extra_msg}
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api createFile(request: OSSCreateFileRequest, runtime: RuntimeOptions): OSSCreateFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/create`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 201)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 指定文件或文件夹路径，删除文件或文件夹
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api deleteFile(request: OSSDeleteFileRequest, runtime: RuntimeOptions): void  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/delete`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 204)) {
      return;
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 获取指定文件或文件夹路径，获取文件或文件夹信息。
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getFile(request: OSSGetFileRequest, runtime: RuntimeOptions): OSSGetFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/get`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 指定文件路径，获取文件下载地址
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getDownloadUrl(request: OSSGetDownloadUrlRequest, runtime: RuntimeOptions): OSSGetDownloadUrlResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/get_download_url`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 可指定分片信息，一次获取多个分片的上传地址。
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getUploadUrl(request: OSSGetUploadUrlRequest, runtime: RuntimeOptions): OSSGetUploadUrlResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/get_upload_url`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 指定父文件夹路径，列举文件夹下的文件或者文件夹
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listFile(request: OSSListFileRequest, runtime: RuntimeOptions): OSSListFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/list`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 列举UploadID对应的已上传分片。
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listUploadedParts(request: OSSListUploadedPartRequest, runtime: RuntimeOptions): OSSListUploadedPartResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/list_uploaded_parts`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 指定源文件或文件夹路径，移动到指定的文件夹。
   * @tags file
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api moveFile(request: OSSMoveFileRequest, runtime: RuntimeOptions): OSSMoveFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/file/move`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 创建共享。
   * @tags share
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api createShare(request: CreateShareRequest, runtime: RuntimeOptions): CreateShareResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/share/create`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 201)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 删除指定共享
   * @tags share
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api deleteShare(request: DeleteShareRequest, runtime: RuntimeOptions): void  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/share/delete`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 204)) {
      return;
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 获取共享信息。
   * @tags share
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getShare(request: GetShareRequest, runtime: RuntimeOptions): GetShareResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/share/get`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 列举指定用户的共享
   * @tags share
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listShare(request: ListShareRequest, runtime: RuntimeOptions): ListShareResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/share/list`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 修改指定共享
   * @tags share
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api updateShare(request: UpdateShareRequest, runtime: RuntimeOptions): UpdateShareResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/share/update`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 列举指定store下的所有文件。
   * @tags store
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error AccessTokenInvalid AccessToken is invalid. {message}
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listStorefile(request: ListStoreFileRequest, runtime: RuntimeOptions): ListStoreFileResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/osspath/store_file/list`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 该接口将会创建用户， 只有管理员可以调用
   * @tags user
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api createUser(request: CreateUserRequest, runtime: RuntimeOptions): CreateUserResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/create`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 201)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 该接口将会删除用户
   * @tags user
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api deleteUser(request: DeleteUserRequest, runtime: RuntimeOptions): void  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/delete`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 204)) {
      return;
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 该接口将会查看用户详细信息
   * @tags user
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api getUser(request: GetUserRequest, runtime: RuntimeOptions): GetUserResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/get`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 该接口将会展示用户列表
   * @tags user
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api listUsers(request: ListUserRequest, runtime: RuntimeOptions): ListUserResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/list`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 该接口将会根据条件查询用户
   * @tags user
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api searchUser(request: SearchUserRequest, runtime: RuntimeOptions): ListUserResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/search`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

  /**
   * 该接口将会更新用户信息
   * @tags user
   * @error InvalidParameter The input parameter {parameter_name} is not valid.
   * @error ForbiddenNoPermission No Permission to access resource {resource_name}.
   * @error NotFound The resource {resource_name} cannot be found. Please check.
   * @error InternalError The request has been failed due to some unknown error.
   * @error ServiceUnavailable The request has failed due to a temporary failure of the server.
   */
  api updateUser(request: UpdateUserRequest, runtime: RuntimeOptions): UpdateUserResponse  {
    var accesskeyId = @getAccessKeyId();
    var accessKeySecret = @getAccessKeySecret();
    var accessToken = @getSysAccessToken();
    protocol = @getProtocol(@protocol, 'https');
    method = 'POST';
    pathname = `/v2/user/update`;
    headers = {
      host = @getHost(@endpoint, `${@domainId}.api.alicloudccp.com`),
      content-type = 'application/json; charset=utf-8',
    };

    if (@notEmpty(accessToken)) {
      headers.authorization = `Bearer ${accessToken}`;
    } else if (@notEmpty(accesskeyId) && @notEmpty(accessKeySecret)) {
      headers.date = @getRFC2616Date();
      headers.accept = 'application/json';
      headers.x-acs-signature-method = 'HMAC-SHA1';
      headers.x-acs-signature-version = '1.0';
      headers.authorization = `acs ${accesskeyId}:${@getSignature(__request)}`;
    }
    body = @toJSONString(request);
  } returns {
    if (@isStatusCode(__response, 200)) {
      return {
        requestId = __response.headers.x-ca-request-id,
        ...@readAsJSON(__response),
      };
    }

    if (@notEmpty(__response.headers.x-ca-error-message)) {
      throw {
        data = {
          requestId = __response.headers.x-ca-request-id,
          statusCode = __response.statusCode,
          statusMessage = __response.statusMessage,
        },
        message = __response.headers.x-ca-error-message,
      };
    }
    throw {
      data = {
        requestId = __response.headers.x-ca-request-id,
        statusCode = __response.statusCode,
        statusMessage = __response.statusMessage,
      },
      ...@readAsJSON(__response),
    };
  } runtime {
    timeouted = 'retry',
    readTimeout = runtime.readTimeout,
    connectTimeout = runtime.connectTimeout,
    localAddr = runtime.localAddr,
    httpProxy = runtime.httpProxy,
    httpsProxy = runtime.httpsProxy,
    noProxy = runtime.noProxy,
    maxIdleConns = runtime.maxIdleConns,
    socks5Proxy = runtime.socks5Proxy,
    socks5NetWork = runtime.socks5NetWork,
    retry = {
      retryable = runtime.autoretry,
      maxAttempts = @defaultNumber(runtime.maxAttempts, 3),
    },
    backoff = {
      policy = @default(runtime.backoffPolicy, 'no'),
      period = @defaultNumber(runtime.backoffPeriod, 1),
    },
    ignoreSSL = runtime.ignoreSSL
  }

}

